---
description: 防止 AI 工具格式化清理污染既有代码风格
globs: app/templates/**/*.html,app/**/*.py
alwaysApply: true
---

# 格式风格保留规则

- 修改任意文件时，只允许改动任务明确要求的那部分内容，不允许对任务范围外的代码做任何格式化清理，包括但不限于：引号风格（单引号/双引号）、缩进字符（空格/Tab）、空行数量、import 语句顺序、行尾空格
- 如果发现既有代码风格不一致或有改进空间，应该单独提出来，不要在执行其他任务时顺手「清理」
- 改动前后对比 diff，如果 diff 里出现了与本次任务描述无关的格式变化行，应该自我修正，去掉这些无关变化

## HTML 可见结构与文案约束（追加 2026-07）

**凡改动 HTML 模板时，以下变更须在设计确认时「逐项列出」，不得在实现时顺手完成：**

| 变更类型 | 要求 |
|---|---|
| 表头文案（`<th>` 文本）变更 | 须在设计"范围"章节列出旧值 → 新值 |
| 列数 / colspan 变化 | 须在"交互契约"行写明列结构变化与原因 |
| 可见按钮/链接文案变更 | 须在设计稿明确标注 |
| CSS class 的增删（影响视觉样式） | 须列出受影响元素与类名 |

**自检要求**：交付前执行 `git diff -- app/templates/` 审查 diff，凡出现上表变更但设计稿中未列出者，须先向用户确认再提交。

**新增 DOM 行禁止越权（2026-08 追加）**：凡在模板中**新增任意可见 DOM 行**（包括 `{% if %}` 渲染块、新增列、新增文本节点），必须在对应设计文档（`doc/design/*.html` 或该轮用户确认回复）中找到对应条目，否则视为越权变更。**强制自检命令**：
```bash
git diff -- app/templates/ | grep '^+' | grep -v '^+++' | grep -E '<(div|span|td|th|tr|button|a|input|label|p|h[1-6])' | head -20
```
若存在未在设计文档中列出的新可见元素，禁止提交，须先提设计方案。

## 颜色约束（追加 2026-07）

- **禁止**在模板和 Vue 组件中新增硬编码十六进制颜色（`#xxxxxx`），必须使用 `var(--cp-*)` 引用 `app/static/css/console-theme.css` 中的变量。
- 交付前执行 `python scripts/audit_hardcoded_colors.py --check`，存在硬编码颜色则不得提交。
- 需要新颜色时，先在 `console-theme.css` 的 `:root` 中添加语义变量定义，再引用。

## 管理端 UI 样式一致性约束（追加 2026-08）

**禁止在 HTML 模板中使用 inline style 设定尺寸/字号/间距等布局属性**（`height`、`width`、`font-size`、`padding`、`margin`、`gap` 等）。须提取为 CSS class，写在 `{% block css %}` 内或 `console-theme.css` 中。

**工具栏/搜索栏标准样式规范**（对照 `cron_list.html` 和 `rbac/users.html`）：

| 属性 | 标准值 |
|---|---|
| 输入框/选择器/按钮高度 | `height: 28px` |
| 字号 | `font-size: 12px` |
| 间距 | `gap: 6px`（表单内控件）/ `gap: 12px`（工具栏块间） |
| 容器 | `display: flex; align-items: center; flex-wrap: wrap;` |
| 统一类名 | 工具栏 `.rbac-toolbar` / 搜索 `.rbac-search-form` |
| 辅助文字 | 使用 `color: var(--cp-muted); font-size: 12px;`，提取为类而非 inline |
| 清除按钮 | `.rbac-search-clear`，不独创新样式 |

**自检要求**：交付前 `git diff -- app/templates/` 若出现 `style="height:` / `style="font-size:` 等 inline 布局属性，须重构为 CSS class。

**表格列变更验收（追加 2026-08）**：凡在管理端模板中新增或删除表格列，必须在 1440px 视口下截图验证：① 所有 `<th>` 无换行；② 横向滚动正常；③ 操作列可达（通过滚动）。验证命令：`browser_cdp Emulation.setDeviceMetricsOverride {width:1440}` → 截图 → `browser_cdp Runtime.evaluate "document.querySelector('.c-table-wrap').scrollWidth > document.querySelector('.c-table-wrap').clientWidth"`（期望 true）。
