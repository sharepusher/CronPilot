---
description: 技术文档双格式、索引与 CI 同步流程
globs: doc/**,README.md,RELEASE_NOTES.md,.github/workflows/**,scripts/html_docs_to_markdown.py
alwaysApply: false
---

# 文档规范

## 双格式（HTML + Markdown）

- 源稿优先维护 **`doc/*.html`**（样式统一、可 `/docs/` 远程访问）。
- 每个 HTML（除 `index.html`）须有同名 **`doc/*.md`**，由脚本生成，勿手写大段重复 MD。
- 手写索引：**`doc/index.md`**（双格式对照表，**改 `index.html` 后须同步**）；**`doc/index.html`** 每卡片提供 HTML · Markdown 链接。
- `html_docs_to_markdown.py --check` 会校验 `index.md` 与 `index.html` 中 Release Notes 版本一致。
- 页脚：各 HTML 页含 `Markdown 版` + `文档索引` 链接（可用 `--footers-only` 补全）。

## 同步命令

```bash
pip install markdownify
python scripts/html_docs_to_markdown.py          # 生成/更新 doc/*.md
python scripts/html_docs_to_markdown.py --check  # CI 同款校验
```

修改 HTML 后 **必须** `--check` 通过再提交。

## 优化与文档顺序（强制）

与 `cronpilot-project.mdc`「设计先行 / 交付闭环」一致：

1. **设计**（清晰完整准确：问题 / 根因 / 方案 / 范围 / **分批** / **验收**；优化与功能开发均须）
2. **用户确认**（确认前不写实现代码；「请完成 XX」不等于已确认）
3. **实现**（按已确认分批，最小 diff）
4. **验证通过**（按已确认验收项 + 项目总则「优化验收」表）
5. **更新文档**（本表所列 HTML/MD + `--check`）
6. **commit**（代码 + 文档；仅用户要求时）

任一步失败：修复 → 重跑验证 → **再次更新相关文档** → 再 commit。  
**不得**在验收未通过时更新 Release/RFC/PRD 等「已交付」表述；**不得**验证通过后不写文档就 commit；**不得**未确认设计就实现。

依赖 Tier、部署脚本、验收脚本变更时，同步 `doc/依赖升级RFC.html` 与 `AGENTS.md` 快速命令（若新增入口）。

## 在线访问

- `http://<host>:5860/docs/` → HTML 索引
- `http://<host>:5860/docs/index.md`、 `/docs/架构设计文档.md` 等

## 文档类型与落点

| 类型 | 落点 |
|------|------|
| 版本变更 | `RELEASE_NOTES.md` + `doc/RELEASE_NOTES.html` |
| 架构/方案/PRD/对比 | `doc/*.html` + 生成 MD |
| 裸机部署 | `README` 部署节 + `doc/非Docker部署指南.html` |
| 测试验收 | `doc/P0测试用例与验收手册.html`、`tests/README.md` |
| 许可 | `doc/LICENSE-AUDIT.html`、`LICENSE`、`NOTICE` |

## 表述

- 文档面向 **CronPilot** 读者；对比上游时陈述事实，不喧宾夺主。
- **对外发行文案**（`RELEASE_NOTES.md` / `doc/RELEASE_NOTES.html`、README 版本摘要、GitHub Release）：
  - 写用户 / 运维可感知的变更、升级步骤与兼容性；
  - **禁止** OPT / Tier / Phase / DEC 等内部编号、设计稿链接堆砌、「已交付/搁置/待收尾」进度口吻；
  - 内部排期与编号放在 `doc/交付状态与路线图.html`、`doc/依赖升级RFC.html`、设计稿。
- 品牌统一 **CronPilot**；避免旧品牌主导表述。
- 新能力在 PRD 中标 OPT 编号（P0/P1/P2）与验收标准，便于与 Plombery 对照文档一致。

## `doc/_pending_sync/` 与删除纪律

- **目录说明**：`doc/_pending_sync/INDEX.md`（勿将目录说明写入 `README.md`——该路径留给根 `README.md` 的 pending 副本）。
- **临时副本**：仅 `sync_all_docs.py` 在 `PermissionError` 时写入，并登记 `pending_apply.manifest`；`apply_pending_docs.sh` 只合并 manifest 路径。
- **删除任何文件前**须在 `doc/_pending_sync/已合并补丁记录.md` 追加：**完整相对路径**（自仓库根）、**充分删除原因**、对照证据；未跟踪文件亦同。提交说明中应复述关键删除项。
- **入库前**：`bash scripts/check_pending_sync.sh`（CI `docs-sync.yml` 同款）。
