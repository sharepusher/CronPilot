# CronPilot Redesign — 第八轮 Mockup 对比评估

> HTML 版：[UI重设计-第八轮Mockup对比评估.html](UI重设计-第八轮Mockup对比评估.html) · [文档索引](../../index.html) · [索引 Markdown](../../index.md)

# CronPilot Redesign — 第八轮 Mockup 对比评估

基准：`doc/design/CronPilot-2026-redesign-mockup.html`（权威内部设计规格）· 评估日期：2026-08-20

**总体对齐进度**：已完成页面 / 核心功能页面 = **4 / 10**已完成：操作记录(Z1)、审计日志(Z2)；待修复：执行记录(A1)、用户管理(A2)、注册审批(A3)、其余页面

## ✅ 已对齐页面（本轮确认）

| 页面 | Mockup 目标 | 当前实现 | 状态 |
| --- | --- | --- | --- |
| **操作记录** | 7 列：操作人｜操作类型｜操作对象｜变更详情｜操作结果｜时间｜来源 IP | ✅ 7 列完全对齐；动作 badge 语义色 | Z1 完成 |
| **审计日志** | 6 列 + 5 chip 快速筛选 | ✅ 6 列完全对齐；全部/登录成功/登录失败/权限拒绝/用户管理 chip | Z2 完成 |

## ⚠️ 待修复偏差

HIGH
A1 — 执行记录：列结构与筛选器全面重对齐

#### 🔴 当前实现（7列）

1. LOG ID （数字 ID）
2. 任务名称
3. 返回内容 （完整响应体）
4. 执行时间
5. 耗时
6. 结果 （单列合并）
7. 操作

当前筛选器（5个toggle按钮）：

非成功
全部
仅失败
仅异常
仅成功

✗ 无失败行背景色高亮

#### 🟢 Mockup 目标（7列）

1. 任务 （task-cell 样式，突出任务名）
2. 触发时间 （mono 字体，muted 色）
3. 耗时
4. 响应码 （HTTP status code）
5. 状态 （健康度 badge：成功/超时/失败）
6. 失败原因 （成功时显示 —）
7. 操作 （仅图标按钮，眼睛图标）

Mockup 筛选器（4个chip）：

全部
失败
超时
业务组 ▾

✓ 失败/超时行背景 danger-bg 高亮

影响文件

`app/templates/redesign/execution_logs.html` · `app/main/views.py`

可能需要修改 `app/repositories/job_log_repository.py` 添加超时/失败筛选支持

⚠️ **注意**：「业务组 ▾」chip 筛选需确认后端是否支持按组过滤执行记录。若后端已支持，则可直接实现；否则此 chip 暂不实现。

MEDIUM
A2 — 用户管理：列顺序 + 列名 + chip 筛选器

#### 🔴 当前实现（9列）

1. 用户名 （+ Avatar）
2. 花名
3. 岗位
4. 角色
5. 状态 ← *（Mockup: 业务组位置）*
6. 密码 ← *（Mockup: 密码状态）*
7. 业务组 ← *（Mockup: 状态位置）*
8. 创建时间
9. 操作 （图标按钮）

✗ 无 chip 筛选器

#### 🟢 Mockup 目标（9列）

1. 用户 （Avatar + 用户名主副标题）
2. 花名
3. 岗位
4. 角色
5. 业务组 （移到角色后）
6. 状态
7. 密码状态 （列名更规范）
8. 创建时间
9. 操作

Mockup chip 筛选器：

全部 6
启用 5
停用 1
角色 ▾
业务组 ▾

改动范围

列顺序调整（业务组↔状态对调）；密码→密码状态重命名

新增 chip 筛选（全部/启用/停用/角色▾/业务组▾）；动态计数显示

HIGH
A3 — 注册审批：缺失 3 列 + 列顺序 + chip 重命名

#### 🔴 当前实现（7列）

1. ID （Mockup 无）
2. 用户名
3. 花名
4. 邮箱 （Mockup: 第1列）
5. 岗位
6. 申请角色 ← *缺失*
7. 业务组 ← *缺失*
8. 申请原因 ← *缺失*
9. 申请时间
10. 操作

当前 chip：

全部
待审批
已通过
已拒绝
已过期

#### 🟢 Mockup 目标（9列）

1. 邮箱 （首列）
2. 用户名
3. 花名
4. 岗位
5. 申请角色 （新增列）
6. 业务组 （新增列）
7. 申请原因 （新增列）
8. 申请时间
9. 操作

Mockup chip（4个，无"全部"）：

待审核 3
已通过
已拒绝
已过期

改动范围

删除 ID 列；调整列顺序；新增申请角色/业务组/申请原因列

默认展示"待审核"chip（与 Mockup 一致）；显示待审批数量 badge

💡 **数据可用性**：`RbacRegistrationRequest` 模型已有 `role`、`reason`、`group_ids`（或关联）字段，新增列只需模板展示。

## 📋 其他页面简评（非本轮优先项）

| 页面 | 主要偏差 | 优先级 |
| --- | --- | --- |
| **任务中心 (Dashboard)** | Stats 卡片布局；任务列表列名（7列）；健康度指标展示 | MEDIUM |
| **执行详情 / Run Inspector** | Inspector 面板结构；日志展示；失败分析区块 | LOW |
| **标签管理** | 按命名空间分组展示（Mockup 有 namespace 分组，当前为平铺） | LOW |
| **业务组** | M1 纯 Mockup 卡片已对齐（上次确认） | 基本对齐 |
| **任务创建/编辑表单** | 字段布局与帮助文本细节 | LOW |

## 🎯 推荐修复顺序

| # | 任务 | 影响范围 | 估计工作量 |
| --- | --- | --- | --- |
| **A1** | 执行记录：列重命名 + 列结构 + chip 筛选器重设计 | `execution_logs.html` + `views.py` + 可能 repo | 中（需后端支持超时筛选） |
| **A3** | 注册审批：增加 3 列 + 调整顺序 + chip 重命名 | `registration_review.html` + `views.py` | 小（模板数据已有，只需展示） |
| **A2** | 用户管理：列顺序 + 列名 + chip 筛选器 | `users.html` + `views.py` | 小～中（chip 需计数逻辑） |

📄 权威 Mockup 来源：`doc/design/CronPilot-2026-redesign-mockup.html` · 评估轮次：第八轮

[文档索引](../../index.html) · [Markdown](UI重设计-第八轮Mockup对比评估.md) · [索引](../../index.html)

---

[← 文档索引（HTML）](../../index.html) · [← 文档索引（Markdown）](../../index.md)
