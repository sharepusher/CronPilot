---
description: 发布、非 Docker 部署与 GitHub 协作流程
globs: gun.py,start.sh,scripts/**,docker-compose.yml,Dockerfile,.github/**
alwaysApply: false
---

# 发布与部署流程

## 非 Docker 部署（标准路径）

**Linux（Ubuntu / CentOS 7·8）：** `sudo bash scripts/install_linux.sh --production` → 配置 MySQL → `bash scripts/run_production.sh`。试用加 `--sqlite`。详见 `INSTALL.md`。

1. `cp conf.ini.example conf.ini` → 配置 DB、`login_pwd`（推荐 `scripts/hash_login_password.py`）、Redis、`block_private_ip`。
2. `bash scripts/cronpilot.sh install` 或 `bash scripts/install_core_deps.sh`（自动 `.venv-py*`） 或 `PY=python3.9 bash scripts/install_core_deps.sh`；生产再 `pip install -r requirements.txt`
3. 生产：`bash scripts/run_production.sh`（内含 `ensure_business_tables.sh`：MySQL/SQLite 建表补列）或 `export FLASK_CONFIG=production` → `gunicorn -c gun.py manage:app`（`0.0.0.0:5860`）。
4. 本地冒烟：`bash scripts/start_local.sh`（默认 `127.0.0.1:5001`）；远程调试改 `host='0.0.0.0'`。

## 访问入口

| 入口 | URL | 认证 |
|------|-----|------|
| 管理端 | `/` | 用户名 + 密码（`rbac_users`；空库种子 `admin`/`login_pwd`；自助改密 `/rbac/password` 或用户管理） |
| API | `/api/...` | `api_access_token` 等 |
| 技术文档 | `/docs/` | **无登录**（公网需 Nginx 鉴权或内网） |

## 发布检查清单

按 **设计 → 实现 → 验证 → 文档 → commit → 打 tag / Release** 顺序（详 `.cursor/rules/cronpilot-project.mdc`）：

- [ ] 设计已确认（UI/行为变更）
- [ ] **验证通过**：`cronpilot.sh test` + `verify_golden_path.sh`；**Docker 须** `verify_cronpilot_docker_mac.sh`（full）**且** `verify_docker_compose.sh`
- [ ] **Release 前文档梳理**（打 tag / 发 GitHub Release **之前**必做）：
  - [ ] 更新 `doc/交付状态与路线图.html`：每项 OPT/RFC/Tier 标「已交付 + 版本」或「设计待确认 / 未开始」
  - [ ] `RELEASE_NOTES.md`：`[Unreleased]` 仅保留下一版草稿；已发布内容下沉到版本节
  - [ ] 相关设计稿页眉（如 P1/UI/RFC）与 `doc/产品优化需求`、`doc/依赖升级RFC`、`doc/详细技术方案` 版本列一致
  - [ ] `doc/index.html` / `index.md` 入口与 README 版本表对齐
- [ ] **文档已同步**：`RELEASE_NOTES` + 相关 `doc/*.html`；`html_docs_to_markdown.py --check` 通过
- [ ] **同一 commit** 含代码与文档（除非用户要求拆 PR）
- [ ] 未提交 `conf.ini`、数据库文件、虚拟环境
- [ ] 对接方已知 `cronpilot_log_id` / `cronpilot_sign` / `add_log` 协议

## GitHub

- 远程：`sharepusher/CronPilot`（`main`）
- CI：**Unit tests**、**Docker install verify**（Ubuntu/Rocky8/CentOS7）（矩阵 3.8–3.11 + core 依赖）、**install-full**（矩阵 3.9/3.10/3.11 全量）、**Docs sync**
- Release 文案可从 `RELEASE_NOTES.md` 摘录；Full Changelog 用 compare 链接

## 生产安全

- `/docs/` 不对公网裸奔；管理端强密码；`block_private_ip=1`。
## 本地 / Docker 预览与进程

- 本地 `start_local_full.sh`：**不**开 Flask reloader；改 `app/` / 模板后须 `bash scripts/cronpilot.sh restart --daemon`，禁止只刷新浏览器（详项目总则「本地进程与热更新」）。
- Docker：改代码或重建镜像后须 `docker compose restart`（或 `--build`），与本地 `:5001` 进程无关。
- 勿对 `0.0.0.0` 开 Flask `debug=True`。
