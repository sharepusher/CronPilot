# CronPilot · 任务生命周期与无删除设计

> HTML 版：[任务生命周期与无删除设计.html](任务生命周期与无删除设计.html) · [文档索引](../index.html) · [索引 Markdown](../index.md)

[← 文档索引](../index.html)
产品约束生命周期

# 任务生命周期与无删除

暂停 ≠ 下线；取消任务/日志人工删除；下线为不可逆终点，同类需求须新建

LIFECYCLE-1：**已落地 · v1.0.0** ·
LIFECYCLE-2：**已落地 · v1.0.0**

**关联：**[RBAC v4](RBAC架构设计方案.html) ·
[架构设计](../arch/架构设计文档.html)（§操作审计 OPT-P1-09）·
[交付状态](../交付状态与路线图.html)

## 一、问题与根因

| 项 | 内容 |
| --- | --- |
| 问题 | 任务/执行流水提供「删除」，易丢失审计与历史；「停止」与「结束」语义混淆 |
| 根因 | 早期 CRUD 把删除当生命周期终点；模型已有 `status=-1` 结束态，但 Web 以物理删除为主 |
| 目标 | 配置与流水可追溯；暂停可恢复；下线不可逆；同类需求新建任务 |
| LIFECYCLE-2 补充 | 下线无原因/时间难查阅；备注可空；无创建/编辑时间戳 |

## 二、状态机（权威）

| 动作 | 含义 | `cron_infos.status` | 可逆？ |
| --- | --- | --- | --- |
| **运行** | 调度执行中 | `1` | — |
| **暂停** | 临时停调度，配置保留 | `0` | 是（可再「运行」） |
| **下线** | 生命周期终点 | `-1` | **否**（不可再启/再改；同类需求**新建**） |

```
创建 → 运行 ⇄ 暂停
         ↘       ↙
          下线（终点）
```

**不做：**管理端/API 人工删除任务；管理端人工删除执行日志/流水；对已下线任务再启动或编辑「复活」。

**保留：**按 `job_log_counts` 的系统自动裁剪（容量策略，非用户删除功能）；one-shot 自然结束后写 `status=-1`（LIFECYCLE-2 同时写系统下线原因与时间）。

## 三、方案要点（LIFECYCLE-1 · 已落地）

### 3.1 Web

- 退役 `/cron_del`、`/cron_batch_del`、`/job_log_delete`、`/job_batch_delete`（返回 410）
- 新增 `/cron_retire`：写 `status=-1` + `remove_job`；**不删** `cron_infos` / `job_log`
- 列表：运行中/已暂停对**所有角色**展示「下线」入口；仅具备 `cron:retire` 的账号可进入下线表单并执行；其它角色（含种子 `admin`）点击弹出「权限不足：当前账号不可下线任务」。已下线仅展示状态，无启动/暂停/编辑/下线
- 已下线禁止 `update_status`、`cron_edit`

### 3.2 API

- `/api/cron/status` 仍仅 `0|1`（暂停/运行）
- 新增 `/api/cron/retire`（按 `task_name`）
- `/api/cron` upsert：若同名任务已 `status=-1` → **拒绝**，提示须新建（新 `task_name`）

### 3.3 RBAC

```
ROLE_PERMISSIONS = {
    'viewer':   {'cron:read', 'log:read'},
    'operator': {'cron:read', 'cron:write', 'log:read', 'operation:read'},
    'admin':    {'cron:read', 'cron:write', 'cron:retire',
                 'log:read', 'operation:read', 'user:manage', 'audit:read'},
  }
  # cron:write = 添加/编辑/启动/暂停；cron:retire = 下线（仅 admin）
  # operation:read = 操作记录（operator+admin）；audit:read = RBAC 审计（仅 admin）
  # 废弃 cron:delete / log:delete
```

`cron:write` = 添加/编辑/启动/暂停；`cron:retire` = 下线（仅 admin）。**废弃** `cron:delete`、`log:delete`。

## 四、LIFECYCLE-2 · 元数据与下线可追溯（已落地 · v1.0.0）

目标：列表/详情可直接查阅下线原因与时间、任务备注与创建/最后编辑时间。**不新建**「任务更新日志」表；完整「谁改了什么」仍走 [OPT-P1-09 `operation_log`](../arch/架构设计文档.html#op-audit-detail)。

### 4.1 `cron_infos` 扩展字段

| 字段 | 类型（建议） | 说明 |
| --- | --- | --- |
| `task_keyword` | VARCHAR(500) | 任务说明/备注；**新建与编辑必填**（1～500 字 trim 非空） |
| `created_at` | VARCHAR(25) | 创建时间；只写一次 |
| `updated_at` | VARCHAR(25) | 最后配置编辑时间；创建时与 `created_at` 相同 |
| `retire_reason` | VARCHAR(500) | 下线原因；人工下线必填（1～500 字 trim 非空；与说明同长） |
| `retired_at` | VARCHAR(25) | 下线时刻 |

**不做 `retired_by`：**操作人属体验增强，与远期 `operation_log` 重叠；列表用原因文案区分人工/系统即可。

### 4.2 校验与行为规则

| 规则 | 内容 |
| --- | --- |
| 备注必填 | Web/API 新建、编辑：`task_keyword` trim 后 1～500 字，否则拒绝 |
| 存量空备注 | 允许继续运行/暂停；**下次编辑必须补全**（避免 bulk 迁移失败） |
| `updated_at` | **仅**添加/编辑配置时更新；暂停/运行/**下线不刷**（下线用 `retired_at`） |
| 人工下线 | Web `POST /cron_retire`、API `/api/cron/retire` 必传 `reason`；写 `status=-1` + `retire_reason`/`retired_at` + `remove_job` |
| 系统下线 | one-shot 结束 / 调度收束：写 `retired_at`；`retire_reason` 固定文案（如「一次性任务执行完成（系统）」） |

### 4.3 UI（对齐 simpleboot：control-group + 行内灰字）

- 添加/编辑：备注标签改为必填；灰字说明「用途/负责人/告警联系等」；去掉「可为空」
- 编辑页：只读展示「创建时间」「上次编辑时间」
- 列表：已下线行展示「下线于 … · 原因 …」（可截断）；可选展示创建时间列
- 下线：确认框或小表单必填原因后再提交（不可一键无因下线）

### 4.4 与审计分工（不做独立更新日志）

| 能力 | 落点 | 状态 |
| --- | --- | --- |
| 列表/详情快速查阅 | `cron_infos` 行内字段（本节） | LIFECYCLE-2 · 已落地 |
| 完整变更历史（old→new、操作人） | `operation_log` · action 含 `create_cron` / `update_cron` / `toggle_status` / `retire_cron` | OPT-P1-09 · 已交付（v1.0.0） |
| RBAC 登录/拒绝 | `rbac_audit_logs` | 已落地；**不**塞业务 diff |

**明确不做：**单独「任务更新日志」表（与 OPT-P1-09 重复）。LIFECYCLE-2 实现期也不强制落地 `operation_log`；远期 `retire_cron` 的 `detail_json` 应含 `reason`。

## 五、开发步骤

### 5.1 LIFECYCLE-1（已完成）

| 步骤 | 内容 |
| --- | --- |
| D0 | 文档冻结（本文 + RBAC/架构/PRD/路线图/Release Notes） |
| D1 | 去掉删除 UI/路由；改单测 |
| D2 | 实现 `cron_retire` + 禁改已下线 + API |
| D3 | policy / 三角色 / smoke |

### 5.2 LIFECYCLE-2（已完成）

| 步骤 | 内容 |
| --- | --- |
| M0 | 本文设计冻结 + 交叉文档同步 |
| M1 | 模型字段 + schema 升级；校验与 service |
| M2 | Web/API retire 必传 reason；自动下线写系统原因 |
| M3 | 模板（必填备注、时间只读、下线原因表单、列表展示）+ 单测/黄金路径 |

## 六、验收

### 6.1 LIFECYCLE-1

1. UI 无任务删除、无日志删除
2. 旧删除 URL → 410
3. 暂停 ⇄ 运行；下线后不可运行/编辑；列表显示已下线
4. 下线后流水仍可查；同类需求走添加（新 task\_name）
5. 三角色分权：operator 可暂停不可下线；admin 可下线
6. 自动日志裁剪仍可按配置工作

### 6.2 LIFECYCLE-2（实现后）

1. 无备注不可新建/编辑；存量空备注编辑时被拒绝直至补全
2. 无 `reason` 不可人工下线；下线后列表/详情可见原因与 `retired_at`
3. 新建可见 `created_at`；编辑后 `updated_at` 变化；暂停不改变 `updated_at`
4. one-shot 结束后有系统原因与下线时间

CronPilot · 任务生命周期与无删除 ·
[Markdown](任务生命周期与无删除设计.md) ·
[索引](../index.html)

---

[← 文档索引（HTML）](../index.html) · [← 文档索引（Markdown）](../index.md)
