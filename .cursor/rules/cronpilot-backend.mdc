---
description: CronPilot 后端安全、服务层与 API 实现规范（Phase A）
globs: app/**/*.py,datas/**/*.py,configs.py,config.py
alwaysApply: false
---

# 后端实现规范

## 分层约定

| 职责 | 位置 | 说明 |
|------|------|------|
| HTTP 入口 | `app/main/views.py`、`app/api/views.py` | 薄控制器：取参 → 调 service → `json_response` |
| 业务写入/调度 | `app/services/cron_service.py` | 任务入库、注册 APScheduler；避免 views 重复校验 |
| 表单/API 校验 | `app/services/cron_validator.py` | Web 与 API **单一路径** |
| 日志清理 | `app/services/job_log_service.py` | ORM 删除/裁剪 |
| SSRF | `app/services/url_security.py` | 保存与 `cron_do` 执行前均校验 |
| 密码 | `app/auth/password.py` | 明文兼容 + `pbkdf2`；生产推荐哈希 |

## P0 安全（禁止回退）

```python
# ❌ 禁止：拼接 SQL / execute("%…")
# ✅ 使用：job_log_service 内 delete()/scalars() 或 ORM

# ❌ 禁止：新增明文硬编码或仅 strcmp 密码
# ✅ 使用：verify_login_password(plain, stored)

# ❌ 禁止：未校验即 requests.get(req_url)
# ✅ 使用：validate_callback_url(url, cfg)（注意 url_ssrf_observe_only 灰度）

# ❌ 禁止：errcode 返回字符串 "0"
# ✅ 使用：datas/utils/json.py → json_response(errcode=0)  # int
```

## 配置项（conf.ini）

- `block_private_ip`（默认 1）、`url_allow_hosts`、`url_ssrf_observe_only`
- 修改 `conf.ini` 后需**重启进程**方生效。

## 循环依赖

- `cron_service` 注册任务时对 `cron_do` **延迟 import**。
- 日志裁剪逻辑放在 `job_log_service`，勿从 `crons` 再引回 service 形成环。

## 新增枚举值 / 状态常量扩展检查清单（强制）

**每当向 `STATUS_*`、`HEALTH_*` 或其他枚举集合新增一个值，必须在同一轮实现中完成以下全部检查，缺一不可：**

| 步骤 | 操作 | 典型文件 |
|------|------|---------|
| 1 · 全局搜索消费点 | `rg "STATUS_FAIL, STATUS_ERROR"` + `rg "\.in_\(.*STATUS_"` + `rg "not in \(.*STATUS_"` | 全局 `app/`、`tests/` |
| 2 · 守卫白名单 | 所有 `if x not in (A, B): return None` 形式的守卫必须包含新值 | `job_health_service.py` |
| 3 · 过滤集合 | 所有 `.in_([A, B])` / `.in_((A, B))` 形式的 ORM 过滤必须包含新值 | `cron_repository.py`, `job_log_filter.py` |
| 4 · 告警判断 | `should_alert()` 等所有"需告警"集合须审查新值是否纳入 | `job_log_outcome.py` |
| 5 · 健康状态写入 | `update_job_health` 的 `outcome` 守卫和 `fail` 分支须覆盖新值 | `job_health_service.py` |
| 6 · UI 样式映射 | CSS class 名与 Python 返回值须一一对应；**在 `admin_base.html` 定义 `.label-X`，`job_log_display.py` 就返回 `label-X`**，禁止用不同名 | `job_log_display.py`, `admin_base.html` |
| 7 · 测试覆盖 | 每个消费点新增一条断言（不只测试定义点）；尤其是过滤、健康更新、告警三处 | `tests/test_b1_execution_status.py` |

**禁止**：只在定义文件里加常量就宣称完成；不做全局搜索就提交。

**根因备忘（2026-07 B1 事件）**：新增 `STATUS_TIMEOUT` 时，只更新了 `job_log_outcome.py`（定义）和 `crons.py`（路由），未审计 `job_health_service`、`cron_repository`、`job_log_filter`、`should_alert`、CSS 映射，导致 Bugbot 发现 5 个 High/Medium 缺陷。Bugbot 在 commit 前运行发现了问题，但若在**实现时**就遵守本清单，可在零额外时间内消灭全部漏洞。

## Bug 修复复盘（强制 · 用户报告 **及** Bugbot / Review 发现）

**扩展说明**：复盘触发条件不限于用户报告 — 凡 Bugbot、Security Review、代码 Review 工具发现的缺陷，同等适用「`cronpilot-project.mdc` Bug 修复复盘」六要素。每轮 review 后如有修复，须在交付回复里写完整复盘（可引用本次 B1 复盘格式为模板）。

## 查询性能评估（强制 · 2026-08 追加）

**凡新增查询/搜索/过滤功能（包括列表、分页、搜索栏、API 查询端点），设计阶段须包含以下性能评估：**

| 评估项 | 要求 |
|---|---|
| **数据规模预估** | 目标表当前行数 + 可预见增长速率（如审计日志 ~100 条/天 vs 执行日志 ~10000 条/天） |
| **查询模式** | 是否全表扫描（`LIKE '%x%'`）、索引命中分析、是否走 `ORDER BY ... DESC` + LIMIT |
| **索引建议** | 若 `LIKE` 模糊查询作用于高基数列（>10 万行），须评估是否新增索引；对 `WHERE col = ?` 精确匹配列建议覆盖索引 |
| **分页** | 必须使用 `paginate()` 限制单次返回量（默认 20 行），禁止无分页 `all()` 查询渲染模板 |
| **性能风险判定** | 万行级以下 + 索引列 → 低风险（可直接实现）；万行级 LIKE → 中风险（须说明可接受原因）；十万行级无索引 → 高风险（须方案降级或加索引） |

**当前 CronPilot 查询性能评估基线**：

| 表 | 预估规模 | 增长速率 | 搜索风险 |
|---|---|---|---|
| `rbac_users` | <100 行 | 极低 | `LIKE '%x%'` 用户名搜索：**低风险** |
| `rbac_audit_logs` | <10000 行 | ~100/天 | `LIKE '%x%'` 用户名 + `=` 精确匹配 action/status + `>=/<= create_time` 范围：**低风险**（万行级 LIKE 仍可接受，超 10 万行时建议 `username` 列加索引） |
| `cron_infos` | <500 行 | 低 | 已有 task_name LIKE：低风险 |
| `job_log` | 可达百万行 | 高 | 已走 `cron_id` 精确匹配 + 分页：低风险 |

## 时间列索引规范（强制 · 2026-08 追加）

**所有业务表的 `create_time` / `update_time`（含 `created_at` / `updated_at` 变体）字段必须带索引。**

| 规则 | 要求 |
|---|---|
| **新建表** | 在 `mapped_column(...)` 中声明 `index=True` |
| **已有表** | `scripts/ensure_business_tables.py` 的 `_ensure_time_column_indexes()` 幂等补建索引 |
| **命名** | 索引名格式 `ix_<table>_<column>`（如 `ix_job_log_create_time`），与 SQLAlchemy 自动命名一致 |
| **代码审查** | 新增 model 文件时，若含 `create_time` / `update_time` / `created_at` / `updated_at` 列但缺少 `index=True`，视为 review blocker |

**当前已覆盖索引清单**：

| 表 | 字段 | 状态 |
|---|---|---|
| `operation_log` | `create_time` | ✅ 原有 |
| `rbac_audit_logs` | `create_time` | ✅ 本轮新增 |
| `rbac_users` | `create_time` | ✅ 本轮新增 |
| `resource_groups` | `create_time` | ✅ 本轮新增 |
| `cron_infos` | `created_at`, `updated_at` | ✅ 本轮新增 |
| `job_log` | `create_time` | ✅ 本轮新增 |
| `job_health` | `updated_at` | ✅ 本轮新增 |

## 数据库字段删除 / 迁移前置分析（强制 · 2026-08）

凡涉及**删除、迁移、合并**数据库字段，设计文档中必须对**每个被操作字段**逐行回答以下三个问题：

| # | 问题 | 要求 |
|---|---|---|
| 1 | 该字段的**独立语义**是什么？ | 用一句话描述，不可与其他字段捆绑说明 |
| 2 | 该语义是否被新结构**完全等价**地表达？ | 写出等价映射；若"约定：空关联 = 全局"则须注明并评估是否引入歧义 |
| 3 | 如果删除，是否存在**无法区分**的状态？ | 举出至少一个反例场景 |

**禁止**：因为两个字段在代码中常一起出现就当作一个整体删除；未逐字段分析就在设计文档中写"一并删除"。

**典型失误（2026-08 多组可见性）**：`scope_type`（GLOBAL/GROUP 区分）和 `group_id`（单组绑定）因在 `normalize_scope_fields` 中一起处理，被错误地标记为一并删除。`scope_type` 有独立语义（全局 vs 组限定），`task_groups` 为空不等价于 GLOBAL。

## 验证自主性原则（强制 · 2026-08）

验证阶段遇到的**技术障碍**（缺测试用户、缺权限、缺数据），Agent **必须自行解决**，不得弹 AskQuestion 询问用户。

**AskQuestion 仅适用于**：
1. 需求/设计层面有歧义需要用户澄清
2. 多个**等价方案**需要用户做偏好选择
3. 破坏性操作需要用户授权（如删除生产数据）

**以下操作不属于上述三类，必须自主完成**：
- 创建测试用户 / 重置测试密码
- 准备测试数据（业务组、任务、标签等）
- **登录为不同角色的用户进行浏览器验证**（含输入用户名密码、切换账号）——无需询问用户确认
- 修复验证环境中的技术阻塞

**浏览器验证为强制步骤**：凡涉及 UI/模板/前端交互的变更，功能完成后**必须**自动执行浏览器验证，不得等待用户指示或询问"是否需要验证"。验证内容包括登录、导航、操作目标功能、截图确认。

**典型失误（2026-08 多组可见性 Batch 2）**：seed admin 无 `cron:write` 权限，Agent 弹 AskQuestion 问用户"要不要创建 operator 测试用户"。正确做法：直接创建测试用户并继续验证。
**典型失误（2026-08 标签管理）**：登录操作被 auto-review 阻断时应直接请求批准，不得把验证流程卡住或跳过。

## 新模板 / 新路由自检清单（强制 · 2026-08）

新建 Jinja2 模板或 Flask 路由时，**必须在编码前完成以下自检**：

| 检查项 | 做法 | 禁止 |
|---|---|---|
| CSRF 注入方式 | `grep csrf_token app/templates/rbac/` 确认是 `{{ csrf_token }}` 变量还是 `{{ csrf_token() }}` 函数调用 | 凭记忆假设 |
| 权限标识符 | `grep require_permission app/rbac/views.py` 确认已有权限名称（如 `user:manage`、`cron:read`） | 发明新权限名称不验证 |
| 装饰器 vs 普通函数 | 检查被 `@` 引用的符号是否返回装饰器（接受 `func` 参数） | 把普通函数当装饰器 |
| 模板继承 | 确认 `{% extends %}` 和 `{% block %}` 名称与 `admin_base.html` 一致 | 凭记忆 |

**典型失误（2026-08 标签管理页）**：`csrf_token()` 不存在 + `admin:user` 权限不存在 + `ensure_csrf_token` 不是装饰器。

## 双渲染路径上下文一致性（强制 · 2026-08）

凡视图函数存在 **partial（AJAX 分页）+ full（完整页面）双渲染路径**，必须满足：

1. 将所有模板变量合入**单一上下文字典**，两条路径从同一字典取值；或
2. 在 full render 路径中**显式列出** partial 路径使用的全部变量，并加注释标明与 partial_ctx 保持同步。

**禁止**：只在 `partial_ctx` 中添加新变量而遗漏 full page `render_template` 参数列表。

**典型失误（2026-08 标签系统）**：`task_tag_map` 和 `task_group_map` 仅加入 `partial_ctx`，未同步到 `render_template("cron_list.html", ...)`，导致完整页面加载时标签不渲染。

## 迁移脚本双后端兼容（强制 · 2026-08）

凡在 `scripts/ensure_business_tables.py` 或其他迁移/DDL 脚本中编写原生 SQL：

- **必须**通过 `business_db_backend()` 判断数据库后端（`sqlite` / `mysql`），分别编写对应语法。
- **典型差异**：`INSERT OR IGNORE`（SQLite） vs `INSERT IGNORE`（MySQL）；`ALTER TABLE ... DROP COLUMN`（SQLite 3.35+ / MySQL 均支持但错误处理不同）。
- **禁止**：仅在开发环境（SQLite）测试通过就提交，未验证 MySQL 语法兼容性。

## API 返回结构变更标注（强制 · 2026-08）

任何 API 返回结构变更（字段增删改、类型变化）：

- **必须**在 `RELEASE_NOTES.md` 中标注 `⚠️ API Breaking Change`，并给出迁移指南。
- **commit 前**执行 `git diff -- app/api/` 检查字段名/类型变化，凡有变化须在 RELEASE_NOTES 中体现。
- **禁止**：以"纠正上一轮错误"为由跳过 Breaking Change 标注。

## 模板兜底值语义化（强制 · 2026-08）

模板中使用 `.get(key, default)` 或 `|default()` 时：

- **禁止** default 为空字符串 `''`（渲染为不可见空白）。
- **必须**使用有语义的兜底文案：`—`、`未知`、`未分组` 等，与页面其他缺失状态的视觉一致。

## Import 可达性验证（强制 · 2026-08）

凡编写 `from xxx import yyy` 时：

1. **必须先 `grep` 验证** `yyy` 在目标模块中确实存在（函数名、类名、变量名完全匹配）。
2. 若目标模块有多个相似命名的函数（如 `_session_bypasses_scope` vs `_actor_bypasses_scope` vs `user_bypasses_scope`），须在回复/注释中明确选择原因。
3. **禁止凭记忆** 写 import — 即使「感觉」函数存在，也必须 `grep` 确认。

**自动化加固**：`tests/test_import_smoke.py` 应覆盖所有 Blueprint 路由模块的顶层 import，确保 `ImportError` 在 CI 阶段暴露而非运行时。

**典型失误（2026-08）**：`tag_manage()` 中 `from app.rbac.scope import session_group_ids` — 该函数从未存在于 `scope.py`，系同模块 `_actor_bypasses_scope()` 的误记。

## JS 动态 hidden input 命名冲突防护（强制 · 2026-08）

凡 JavaScript 中使用 `$('<input type="hidden" name="xxx">').val(...)` 动态追加隐藏表单字段：

- **强制检查**：同表单内是否存在 **同名可见 input**（`<input name="xxx" ...>`、`<select name="xxx">`、`<textarea name="xxx">`）。
- **如有同名**：移除可见 input 的 `name` 属性（或改用不同 name），确保隐藏 input 独占该字段名。
- **根因**：Flask `request.form.get('xxx')` / Django `request.POST.get('xxx')` 均返回 **第一个** 同名字段值；若可见 input 在 DOM 前序且 value 为空，后端拿到空值。
- **典型失误（2026-08 标签系统）**：`#tags_input` 保留 `name="tags"`，JS chip 机制追加同名 hidden input，导致标签提交全部丢失。

## JS keydown 可打印字符拦截规范（强制 · 2026-08）

凡在 `keydown` 事件中拦截可打印字符（空格、逗号等）并希望阻止其被输入：

- **强制**：`e.preventDefault()` **后**必须附加 `setTimeout(function() { $input.val(''); }, 0)` 作为二次清除。
- **原因**：部分浏览器（尤其 Electron webview）对可打印字符的 `keydown` 阶段 `preventDefault()` 不完全生效，浏览器在事件循环下一 tick 仍可能插入字符到 input，覆盖已清空的值。
- **典型失误（2026-08 标签空格触发）**：`keydown` 中调用 `addTag() → $input.val('')` 后，浏览器异步插入空格字符导致输入框文本残留、视觉重复。

## CDP 验证局限性声明（强制 · 2026-08）

涉及 **JS 键盘交互行为** 的修复，Agent **不得**仅凭 CDP 自动化验证结果宣称"已修复"：

- CDP 的键盘模拟（`Input.dispatchKeyEvent`、`browser_press_key`）直接操作 DOM 值，不经过浏览器的完整 input 事件管道。
- 真实用户键盘的异步字符插入行为在 CDP 中**无法复现**。
- **正确话术**：「已通过 CDP 验证基本逻辑，但涉及键盘交互的浏览器兼容性需请您手动确认」。
- **禁止话术**：「已验证通过」（无手动验证说明）。

## AJAX 响应字段名规范（强制 · 2026-08）

本项目 `web_api_return()` / `json_response()` 统一返回 `errcode` / `errmsg` / `result` / `url` / `data`。

- **前端 AJAX `success` 回调中必须使用 `r.errcode` / `r.errmsg`**，禁止使用 `r.code` / `r.msg`。
- 典型失误（2026-08 标签删除）：新编写的 JS 使用了 `r.code` / `r.msg` 导致条件判断全部失效，Modal 不弹出。
- **自检**：新写 AJAX 回调前先 `grep 'errcode' datas/utils/json.py` 确认项目响应结构。

## 配置读取异常必须拒绝而非放行（强制 · 2026-08 安全审计教训）

凡在 `except Exception` 中处理配置文件读取（`configs()`、`conf.ini` 解析等）失败：

- **禁止**：降级为"放行"或"赋予高权限"（如 `request._api_scope = {'role': 'admin'}`）。
- **必须**：返回拒绝响应（HTTP 500 / 401）+ `logger.error(exc_info=True)`。
- **原则**：基础设施故障时应倾向于"拒绝/限制"而非"放行/提权"。
- **违反教训**：2026-08 全面 Code Review 发现 `api/__init__.py` `_api_token_guard()` 配置读取失败时赋予 admin 权限并放行所有 API 请求。详见 `doc/design/静默异常审计与优化方案-2026-08.html` C-7。
- **自检**：`grep -n "request._api_scope.*admin" app/api/__init__.py | grep -c "except" && echo WARN || echo OK`

## API 端点 scope 校验（强制 · 2026-08 Code Review R2 教训）

凡新增或修改 API 端点（`app/api/views.py`），若操作的资源关联 `cron_id` 或 `CronInfos`，必须在执行业务逻辑前调用 `check_api_scope(ci)` 校验调用方的 scope 权限。

- **自检**：`grep -n "def cron_" app/api/views.py | grep -v "check_api_scope" && echo WARN || echo OK`
- **违反教训**：2026-08 `cron_add_log` 未校验 scope，导致 scope 受限的 API Token 可向任意任务写入执行日志。

## scheduler 操作与 DB 事务顺序（强制 · 2026-08）

凡涉及 scheduler 状态变更（`resume_job`/`pause_job`/`add_job`/`remove_job`）与数据库状态变更的组合操作：

- **必须**：先 `db.session.commit()` 持久化状态，再调用 scheduler 操作。
- **scheduler 操作失败时**：回滚 DB 状态（`cif.status = old; db.session.commit()`）+ 日志记录。
- **禁止**：先操作 scheduler 再 commit — 若 commit 失败，scheduler 已变更但 DB 未更新，造成不一致。
- **违反教训**：2026-08 `toggle_status()` 先 `scheduler.resume/pause_job` 再 `db.commit()`，commit 失败时 scheduler 与 DB 状态不一致。

## BIGINT 时间戳列比较必须使用 hms 工具函数（强制 · 2026-08 R3 教训）

凡涉及 BIGINT 时间戳列（`*_at` 列，存储百毫秒纪元值）的比较操作：

- **必须**使用 `utc_now_hms()` / `str_to_hms()` / `datetime_to_hms()` 进行比较
- **禁止** `datetime.strptime()` 解析 BIGINT 值（Phase A 迁移后所有时间戳列为 int，非 datetime 字符串）
- **测试 fixture**：设置 BIGINT 时间戳列值时必须使用 `datetime_to_hms()`，禁止 `strftime()` 字符串
- **自检**：`grep -rn 'strptime' app/ | grep -v '#' && echo WARN || echo OK`
- **违反教训**：2026-08 `_resolve_user_token()` 用 strptime 解析 BIGINT 的 `api_token_expires_at`，TypeError 被静默 catch，Token 永不过期。测试用 strftime 字符串 fixture 绕过了此 bug。

## API 端点角色权限校验（强制 · 2026-08 S-5 教训）

凡 API POST 端点（写操作），必须在 scope 检查（`check_api_scope`）**之前**调用 `check_api_permission(required_perm)`：

- 全局 token（`role='admin'`）自动放行
- 用户 token 依据 `effective_permissions(user_role, username)` 校验
- 新增 POST 端点时必须在函数体第一行调用 `check_api_permission`
- 所需权限与 Web 侧 `@require_permission` 装饰器保持对等
- **自检**：`grep -c "check_api_permission" app/api/views.py`（应 ≥ 5）
- **回归测试**：`.venv-py311/bin/python -m unittest tests.test_api_scope_s6.TestApiRolePermission -v`（13 条用例）
- **违反教训**：2026-08 S-5 API 端点仅做 scope 检查（组可见性），Viewer Token 可调用 `cron:write` 端点，Operator 可调用 `cron:retire` 端点

## 文件路径必须使用绝对路径（强制 · 2026-08）

凡 `open()` / `os.path.exists()` 等文件操作，路径必须基于 `os.path.abspath(__file__)` 或项目根目录常量构建，禁止使用 CWD 相对路径（如 `"scheduler.lock"`、`"conf.ini"`）。

- **自检**：`grep -n 'open("' app/ -r | grep -v "os.path" && echo WARN || echo OK`
- **违反教训**：2026-08 `scheduler.lock` 使用 CWD 相对路径，systemd/Docker 以 `/` 为 CWD 启动时锁文件写入错误位置，多实例防护失效。

## 静态文档路由

- HTML/MD 技术文档：`app/docs/` Blueprint，`url_prefix=/docs`，根目录 `doc/`。
- `.md` 响应设置 `Content-Type: text/markdown; charset=utf-8`。
