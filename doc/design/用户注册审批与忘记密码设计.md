# 用户注册审批与忘记密码设计

> HTML 版：[用户注册审批与忘记密码设计.html](用户注册审批与忘记密码设计.html) · [文档索引](../index.html) · [索引 Markdown](../index.md)

设计稿已交付

# 用户注册审批与忘记密码设计

编号：OPT-P1-10 · 日期：2026-08-03 · 状态：**Batch 1–4 已交付**

## 一、问题

当前系统存在两个用户体验缺陷：

1. **新用户无法自助申请加入**：所有用户必须由 admin 手动创建（`/rbac/users_add`），潜在用户需线下联系 admin，效率低。
2. **忘记密码无引导**：登录页（`/rbac/login`）无任何密码恢复提示，用户不知如何处理。

## 二、根因

系统初期以 admin 单点管控为核心设计，未设计自助注册流程和密码恢复引导。登录页仅有用户名 + 密码输入，无辅助入口。

## 三、方案

### 3.1 注册审批流（方案 A）

#### 3.1.1 业务流程

```
[登录页] → 点击「注册账号」
    → [注册申请表单] → 填写邮箱/密码/角色/业务组/缘由 → [提交]
        → 系统创建 pending 申请记录
        → 跳转登录页，提示「申请已提交，审批通过后使用注册账号密码登录」

[登录页] → 用户尝试登录（pending 状态）
    → 系统识别该用户名有 pending 申请 → 显示「您的注册申请正在审核中，请耐心等待。」
    → 系统识别该用户名有 rejected 申请 → 显示「申请已被拒绝。原因：xxx」+ 重新申请链接

[管理端] → admin 导航「注册审批」
    → 查看待审批列表 → [批准] 或 [拒绝]
    → 批准：自动创建 rbac_users + user_groups + API Token，用户可立即登录
    → 拒绝：标记 rejected + 备注原因，用户可重新申请
```

#### 3.1.2 用户名规则

注册时填写**完整公司邮箱地址**，系统自动提取 `@` 前部分作为用户名。邮箱地址保存到 `rbac_users.email` 字段（新增列），供后续通知等扩展使用。

#### 3.1.3 注册申请表单字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 公司邮箱 | ✅ | 格式校验（含 `@`）；自动提取前缀作为用户名；唯一性校验（用户名不可重复） |
| 密码 | ✅ | 用户自设（非默认 changeme），至少 6 位 |
| 确认密码 | ✅ | 二次确认，须与密码一致 |
| 申请角色 | ✅ | 下拉：operator / viewer / admin（admin 选中时显示引导提示但允许提交；审批时校验审批者范围） |
| 目标业务组 | ✅ | 多选下拉，列出所有已有业务组（至少选 1 个） |
| 申请缘由 | ✅ | 文本域，最长 500 字符 |

#### 3.1.4 审批权限

| 审批者类型 | 可见范围 |
| --- | --- |
| 种子 admin（`username='admin'`） | 所有申请 |
| `__ALL__` 管理员 admin | 所有申请 |
| 组管理员 admin | 仅 `group_ids` 与自己组有交集的申请 |

#### 3.1.5 审批通过后动作

- 自动创建 `rbac_users` 记录（使用申请时的密码哈希 + 邮箱）
- 绑定 `user_groups`（根据申请的 group\_ids）
- 自动签发 API Token（复用 `_auto_issue_token()`）
- **不设 `must_reset_password`**（密码已是用户自设）
- 写审计日志：`action='user:register_approve'`

#### 3.1.6 Admin 角色注册（已更新）

admin 角色**可通过注册流程申请**，但审批时有额外的范围约束：

- 注册表单中 admin 选项可见，选中后显示引导提示：「管理员角色申请将由系统管理员审批，审批周期可能较长。」（但不禁用提交按钮）
- 审批时校验审批者的业务组范围 ≥ 申请者请求的业务组（`_check_admin_approval_scope()`）
- 种子 admin 和拥有「全部」业务组的管理员可审批任何 admin 申请
- 按组管理员若业务组范围不覆盖申请者的请求组，则在审批列表中不可见该申请

#### 3.1.7 停用不可恢复与重新注册（已补充）

用户一旦被停用（`is_active=0`），**不可通过任何路径恢复启用**（"停用不可恢复"策略）。如需使用系统，必须：

- 通过注册流程重新申请（同一邮箱/用户名可重新提交）
- 或由管理员在用户管理中重新创建

**技术实现**：

- `set_user_active()`：拒绝 `is_active=1` 请求
- `update_user()`：检查 `is_active == 0` 的用户不可被改为 `is_active=1`
- `users_set_active()` 视图：在入口处拦截 `is_active=1`（纵深防御）
- 编辑页 UI：停用用户显示「已停用（不可恢复）」文本，无恢复按钮

**重新注册流程**：

- `submit_registration()` 仅检查 `is_active=1` 用户的用户名冲突 → 已停用用户名可重新申请
- `approve_registration()` 审批通过时自动删除同名已停用旧记录（释放 UNIQUE 约束），再创建新用户
- `create_user()`（管理员手动创建）同理：仅检查启用用户，自动替换停用旧记录
- 审批页面提示「⚠ 曾被停用」（当待审批用户名存在已停用记录时）

#### 3.1.8 Admin 角色审批权限（已补充）

| 审批者类型 | 可审批 admin 角色申请？ | 条件 |
| --- | --- | --- |
| 种子 admin | ✅ | 无条件 |
| `__ALL__` 管理员 admin | ✅ | 无条件 |
| 组管理员 admin | ✅（有条件） | 审批者业务组 ⊇ 申请者请求的业务组 |
| operator / viewer | ❌ | 无 `user:manage` 权限 |

#### 3.1.9 防恶意注册

| 威胁 | 防护措施 |
| --- | --- |
| 批量提交（Bot） | 同一 username pending 状态仅允许 1 条；CSRF Token 校验 |
| 数据库膨胀 | pending 申请 30 天未审批自动标记 expired；启动时清理 |
| 用户名抢注 | rejected/expired 的 username 释放，可重新申请 |
| 信息泄露 | 登录时仅 username 精确匹配且有 pending/rejected 记录时才提示注册状态 |

#### 3.1.10 性能评估

| 维度 | 分析 | 风险 |
| --- | --- | --- |
| 表规模 | `rbac_registration_requests` 预估几十到几百条（内部系统） | 极低 |
| 登录时额外查询 | 仅在「用户名不存在」时才查注册表（`WHERE username=? AND status IN ('pending','rejected')`） | 极低 |
| 审批列表查询 | admin 分页查询，有 status + create\_time 索引 | 极低 |
| 业务组列表 | 复用已有 `list_resource_groups()` | 极低 |

### 3.2 忘记密码（方案 B：纯提示）

- 登录页新增「忘记密码？」链接
- 点击后展示独立提示页面：「请联系您所在业务组的管理员重置密码。如不确定管理员是谁，请联系系统管理员。」
- **不实现邮件重置**（零外部依赖）

### 3.3 注册进度查询

采用**登录时自动提示**方案（参考 GitLab Self-Managed / Discourse / Keycloak）：

- 用户用 pending 账号尝试登录 → 系统自动显示审核状态
- 无需独立查询页面，登录页本身即为进度查询入口
- 优势：零额外入口、交互最自然、信息泄露面小

## 四、数据模型变更

### 4.1 新表 `rbac_registration_requests`

| 列名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | Integer | PK, AUTO\_INCREMENT | 主键 |
| email | VARCHAR(128) | NOT NULL, INDEX | 申请邮箱 |
| username | VARCHAR(64) | NOT NULL, INDEX | 从邮箱前缀提取的用户名 |
| password\_hash | VARCHAR(255) | NOT NULL | 密码哈希（与 rbac\_users 同算法） |
| role | VARCHAR(20) | NOT NULL | 申请角色（operator/viewer/admin） |
| group\_ids | VARCHAR(255) | NOT NULL | 目标业务组 ID（逗号分隔，如 "1,3"） |
| job\_title | VARCHAR(64) | NOT NULL, DEFAULT '' | 岗位类型：tech/ops/qa/pm/proj\_mgr/strategy/operation/other:自定义 |
| nickname | VARCHAR(64) | NOT NULL, DEFAULT '' | 花名（内部昵称） |
| reason | VARCHAR(500) | NOT NULL | 申请缘由 |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'pending', INDEX | pending / approved / rejected / expired |
| reviewer\_id | Integer | NULL | 审批人 user\_id |
| review\_comment | VARCHAR(500) | NULL | 审批备注 |
| create\_time | VARCHAR(25) | NOT NULL, INDEX | 申请时间 |
| update\_time | VARCHAR(25) | NULL | 审批/过期时间 |
| pending\_username | VARCHAR(64) | NULL, UNIQUE | 竞态防护：pending 时设为 username（保证同一 username 仅 1 条 pending），其他状态设为 NULL |

### 4.2 `rbac_users` 新增列

| 列名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| email | VARCHAR(128) | NULL, INDEX | 用户邮箱（注册时填入，存储供后续扩展） |
| job\_title | VARCHAR(64) | NULL | 岗位类型（注册时必填；管理员创建用户时可为空，用户首次登录后自行补充） |
| nickname | VARCHAR(64) | NULL | 花名（注册时必填；管理员创建用户时可为空，用户首次登录后自行补充） |

### 4.3 岗位类型枚举

| 代码 | 显示名 |
| --- | --- |
| tech | 技术 |
| ops | 运维 |
| qa | 测试 |
| pm | 产品 |
| proj\_mgr | 项目经理 |
| strategy | 策略 |
| operation | 运营 |
| other:自定义内容 | 其他（用户输入，限 20 字符） |

## 五、页面设计

### 5.1 登录页更新（`login.html`）

在现有表单下方新增辅助链接行和状态提示区域：

┌────────────────────────────────────────────────────┐
│ CronPilot 定时调度平台 │
│ 方便、统一、自由 │
├────────────────────────────────────────────────────┤
│ │
│ [用户名输入框] [密码输入框👁] [确定] │
│ │
│ ┌─────────────────────────────────────────┐ │
│ │ ⚠ 您的注册申请正在审核中，请耐心等待。 │ ←条件展示│
│ └─────────────────────────────────────────┘ │
│ │
│ 没有账号？注册账号 忘记密码？ │
│ │
└────────────────────────────────────────────────────┘

**新增元素**：

- 辅助链接行：`font-size: 0.9em; color: var(--cp-muted); text-align: center; margin-top: 12px`；链接色 `var(--cp-accent-blue)`
- 状态提示框（仅在用户名匹配 pending/rejected 申请时展示）：

| 场景 | 提示文案 | 样式 |
| --- | --- | --- |
| 正常登录失败 | 「用户名或密码错误」 | 红色文字（现有不变） |
| pending 注册 | 「您的注册申请正在审核中，请耐心等待。」 | `background: var(--cp-warning-bg); border: 1px solid var(--cp-warning-border); color: var(--cp-warning-text)`；圆角 6px；padding 10px 16px |
| rejected 注册 | 「您的注册申请已被拒绝。原因：{comment}」+ [重新申请] 链接 | `background: var(--cp-danger-bg); border: 1px solid var(--cp-danger-border); color: var(--cp-danger)` |

### 5.2 注册申请页面（`/rbac/register` → `register.html`）

独立页面（不使用 `admin_base.html`），与 `login.html` 同级风格。

┌────────────────────────────────────────────────────┐
│ CronPilot 定时调度平台 │
│ 用户注册申请 │
├────────────────────────────────────────────────────┤
│ │
│ 公司邮箱 \* [ @corp.com ] │
│ ↳ 邮箱前缀将作为登录用户名 │
│ │
│ 密 码 \* [ 👁 ] │
│ ↳ 至少 6 位 │
│ │
│ 确认密码 \* [ 👁 ] │
│ │
│ 花 名 \* [ ] │
│ ↳ 内部昵称 │
│ │
│ 岗位类型 \* [ 技术 ▾ ] │
│ 技术 / 运维 / 测试 / 产品 / 项目经理 │
│ / 策略 / 运营 / 其他 │
│ │
│ ┌──────────────────────────────────────────┐ ←选择 │
│ │ 具体岗位 \* [ ]（限 20 字） │ "其他" │
│ └──────────────────────────────────────────┘ 时展示 │
│ │
│ 申请角色 \* [ operator ▾ ] │
│ ↳ operator 可写任务；viewer 仅查看 │
│ │
│ ┌──────────────────────────────────────────┐ │
│ │ ℹ 管理员角色申请将由系统管理员审批， │ ←admin │
│ │ 审批周期可能较长。 │ 选中时 │
│ └──────────────────────────────────────────┘ │
│ │
│ 目标业务组 \* ┌────────────────┐ │
│ │ □ 研发组 │ │
│ │ □ 运维组 │ │
│ │ □ 数据组 │ │
│ └────────────────┘ │
│ ↳ 至少选择一个业务组 │
│ │
│ 申请缘由 \* ┌────────────────┐ │
│ │ │ │
│ │ │ │
│ └────────────────┘ │
│ ↳ 请简述申请原因（最多 500 字） │
│ │
│ [提交申请] [返回登录] │
│ │
└────────────────────────────────────────────────────┘

**模板结构**：独立 HTML 页面（类似 `login.html`），不使用 `admin_base.html`。表单区域居中，max-width: 600px。

**表单布局**：使用 `control-group` + `controls`（对齐 `users_add.html` 样式）。

**交互方式**：普通 HTML POST 表单，提交后重定向到登录页（`/rbac/login?msg=...`）。

**Admin 角色联动（JavaScript）**：选中 admin 时显示引导提示（不禁用提交按钮）。

```
roleSelect.addEventListener('change', function() {
    if (this.value === 'admin') {
        adminWarning.style.display = 'block';
    } else {
        adminWarning.style.display = 'none';
    }
});
```

**邮箱字段**：`<input type="email" name="email" required placeholder="your.name@company.com">`

### 5.3 忘记密码提示页面（`/rbac/forgot_password` → `forgot_password.html`）

极简独立页面（与 `login.html` 同级）：

┌────────────────────────────────────────────────────┐
│ CronPilot 定时调度平台 │
│ 忘记密码 │
├────────────────────────────────────────────────────┤
│ │
│ ┌──────────────────────────────────────────┐ │
│ │ ℹ️ 如忘记密码，请联系您所在业务组的管理员 │ │
│ │ 进行密码重置。 │ │
│ │ │ │
│ │ 如不确定管理员是谁，请联系系统管理员。 │ │
│ └──────────────────────────────────────────┘ │
│ │
│ [返回登录] │
│ │
└────────────────────────────────────────────────────┘

**样式**：提示框使用 `background: var(--cp-accent-bg); border: 1px solid var(--cp-accent-border); border-radius: 8px; padding: 20px 24px`。ℹ️ 图标使用 `<i class="fa fa-info-circle" style="color: var(--cp-accent-blue)">`。

### 5.4 注册审批管理页面（`/rbac/registration_review` → `registration_review.html`）

管理端标准布局（`extends admin_base.html`），使用导航栏 + 表格。

┌────────────────────────────────────────────────────────────────────┐
│ [任务中心] [任务添加] ... [用户管理] [业务组] [注册审批🔴] [审计] ...│
├────────────────────────────────────────────────────────────────────┤
│ │
│ 筛选: [全部] [待审批] [已通过] [已拒绝] │
│ │
│ ┌────┬──────────┬──────┬──────┬──────┬────────┬────────┬──────┐ │
│ │ ID │ 邮箱 │用户名 │ 角色 │业务组 │申请缘由 │申请时间 │ 操作 │ │
│ ├────┼──────────┼──────┼──────┼──────┼────────┼────────┼──────┤ │
│ │ 3 │a@corp.com│ a │oper │研发组 │接入任.. │08-03 │[✓][✗]│ │
│ │ 2 │x@corp.com│ x │viewer│运维组 │查看日.. │08-02 │已通过 │ │
│ │ 1 │m@corp.com│ m │oper │数据组 │协助部.. │08-01 │已拒绝 │ │
│ └────┴──────────┴──────┴──────┴──────┴────────┴────────┴──────┘ │
│ │
│ [上一页] 1/2 [下一页] │
└────────────────────────────────────────────────────────────────────┘

**表头与列**：

| 列 | 内容 | 备注 |
| --- | --- | --- |
| ID | 申请编号 | 60px |
| 邮箱 | 申请邮箱 | auto |
| 用户名 | 自动提取的用户名 | 100px |
| 角色 | operator / viewer / admin | 80px |
| 业务组 | 组名列表（逗号分隔） | auto |
| 申请缘由 | 截断展示 + hover 全文（title 属性） | auto，max-width: 200px |
| 申请时间 | create\_time | 130px |
| 操作 | 按状态显示不同内容 | 120px |

**操作列**：

| 状态 | 展示 |
| --- | --- |
| pending | [批准] 按钮（`btn btn-success btn-mini`）+ [拒绝] 按钮（`btn btn-danger btn-mini`） |
| approved | 灰色标签「已通过」+ 审批人用户名 |
| rejected | 灰色标签「已拒绝」+ hover 显示拒绝原因（title 属性） |
| expired | 灰色标签「已过期」 |

**操作交互**：

- 批准：`confirm('确认批准该注册申请？')` → POST `/rbac/registration_review`
- 拒绝：`prompt('请输入拒绝原因（可选）：')` → POST `/rbac/registration_review`
- 使用 Ajax 表单（`js-ajax-form` + `js-ajax-submit`），操作后刷新当前页

**导航栏更新（`_nav.html`）**：在 `user:manage` 权限区块内新增 tab，带待审批数量 badge：

```
<li>
  <a href="...">注册审批
    {% if pending_count %}<span class="badge" style="background:var(--cp-danger-fill);color:#fff;margin-left:4px">{{ pending_count }}</span>{% endif %}
  </a>
</li>
```

**Scope 过滤**：种子 admin / `__ALL__` admin 看所有；组 admin 仅看 group\_ids 有交集的申请（admin 角色申请仅当审批者业务组 ⊇ 请求组时才可见）。

**"曾被停用"提示**：当待审批用户名存在 `is_active=0` 的同名记录时，在用户名旁显示 ⚠ 曾被停用，审批弹窗中也显示替换旧记录的警告。

## 六、审计日志

注册与审批全流程纳入审计日志（`rbac_audit_logs`），确保可追溯：

| 事件 | action | resource | actor (user\_id / username) | status |
| --- | --- | --- | --- | --- |
| 提交注册申请 | `user:register_apply` | 申请的用户名 | `user_id=0, username='anonymous'`（申请者无账号） | allow |
| 审批通过 | `user:register_approve` | 申请的用户名 | 审批 admin 的 session 信息（自动） | allow |
| 审批拒绝 | `user:register_reject` | 申请的用户名 | 审批 admin 的 session 信息（自动） | deny |
| 自动过期 | `user:register_expire` | 申请的用户名 | `user_id=0, username='system'`（系统操作） | allow |

**说明**：

- 注册申请提交时申请者还没有账号，`user_id` 用 `0`，`username` 用 `'anonymous'`。`resource` 字段记录申请的用户名，可追溯。
- 审批操作由已登录 admin 触发，复用 `write_audit_log()` 的 session 信息（自动获取 `user_id` / `username` / `ip` / `actor_group_ids`）。
- `AUDIT_ACTION_LABELS` 中新增中文标签映射：`'user:register_apply': '注册申请'`、`'user:register_approve': '审批通过'`、`'user:register_reject': '审批拒绝'`、`'user:register_expire': '注册过期'`。

## 七、范围

| 类型 | 文件 | 操作 |
| --- | --- | --- |
| 模型 | `datas/model/rbac_registration_request.py` | 新建 |
| 模型 | `datas/model/rbac_user.py` | 新增 email / job\_title / nickname 列 |
| Repository | `app/repositories/registration_request_repository.py` | 新建 |
| 服务层 | `app/rbac/services.py` | 新增注册申请/审批函数 |
| 视图 | `app/rbac/views.py` | 新增 register / forgot\_password / registration\_review 路由 |
| 模板 | `app/templates/rbac/register.html` | 新建 |
| 模板 | `app/templates/rbac/registration_review.html` | 新建 |
| 模板 | `app/templates/rbac/forgot_password.html` | 新建 |
| 模板修改 | `app/templates/rbac/login.html` | 增加注册/忘记密码入口 + 状态提示 |
| 导航修改 | `app/templates/rbac/_nav.html` | admin 增加「注册审批」菜单 |
| DDL | `scripts/ensure_business_tables.py` | 新增表建表 + email/job\_title/nickname 列补充 |
| 测试 | `tests/test_registration.py` | 新建 |

**明确不做**：邮件通知/邮件重置、admin 自注册、验证码、注册申请编辑功能、IP 级频率限制（后续可加）

## 八、分批

| 批次 | 内容 | 验收 |
| --- | --- | --- |
| **Batch 1** | 数据模型（`RbacRegistrationRequest` + `rbac_users` 新增 email/job\_title/nickname）+ Repository + 服务层（CRUD + 审批逻辑 + 过期清理 + 岗位类型枚举 + other:自定义校验）+ DDL + 单元测试 | `cronpilot.sh test` 通过 |
| **Batch 2** | 注册表单页面（`/rbac/register`）+ 登录页入口链接 + 忘记密码提示页（`/rbac/forgot_password`）+ 登录时状态提示 | restart → 浏览器可访问注册表单、忘记密码页、登录页有入口链接 |
| **Batch 3** | 审批管理页（`/rbac/registration_review`）+ 导航菜单 + badge + 批准/拒绝操作 + Scope 过滤 | admin 可审批，通过后新用户可登录 |
| **Batch 4** | 端到端验收 + 文档更新（RELEASE\_NOTES + 设计文档状态 + 路线图） | 完整流程走通 + 文档 CI 通过 |

## 九、验收

| # | 步骤 | 期望结果 |
| --- | --- | --- |
| 1 | 打开登录页 | 看到「注册账号」和「忘记密码？」链接 |
| 2 | 点击「忘记密码？」 | 显示联系管理员提示页面 |
| 3 | 点击「注册账号」→ 填写 `test@corp.com`、密码、角色 operator、选择业务组、填写缘由 → 提交 | 跳转登录页，提示「注册申请已提交，审批通过后使用注册账号密码登录」 |
| 4 | 用 `test` + 注册密码登录 | 显示「您的注册申请正在审核中，请耐心等待。」 |
| 5 | 用有权限的 admin 登录 → 导航「注册审批」 | 看到待审批列表，含 `test@corp.com` 的申请 |
| 6 | 点击「批准」 | 申请状态变为 approved |
| 7 | 用 `test` + 注册密码登录 | 成功登录，角色和业务组符合申请 |
| 8 | 注册时选 admin 角色 | 提示框出现，提交按钮禁用 |
| 9 | 注册已存在的用户名（邮箱前缀重复） | 提示「用户名已存在」 |
| 10 | 组 admin 审批页面 | 仅看到自己组相关的申请 |
| 11 | 拒绝一个申请（带原因）→ 用该用户名登录 | 显示「申请已被拒绝。原因：xxx」+ 重新申请链接 |

## 十、交互契约

| 页面 | 方式 | 样板 |
| --- | --- | --- |
| 注册表单 | 普通 HTML POST 表单，提交后重定向登录页 | login.html |
| 忘记密码 | 纯静态提示页，无表单 | 404\_guest.html |
| 审批管理 | Ajax 表单（`js-ajax-form` + `js-ajax-submit`） | cron\_retire.html |

## 十一、样式约束

| 项 | 约束 |
| --- | --- |
| 颜色 | 全部使用 `var(--cp-*)` 变量，零硬编码 |
| 布局 | 登录/注册/忘记密码：独立页面（同 login.html）；审批：admin\_base.html + \_nav.html |
| 表单 | `control-group` + `controls`（同 users\_add.html） |
| 按钮 | `btn btn-primary`（主）、`btn`（次）、`btn btn-success`/`btn btn-danger`（操作） |
| 图标 | Font Awesome 4.4：fa-info-circle、fa-check、fa-times、fa-eye/fa-eye-slash |

## 十二、风险

| 风险 | 缓解 |
| --- | --- |
| 恶意批量申请 | 同一 username pending 仅允许 1 条；30 天自动过期 |
| 审批遗漏 | 导航 badge 显示待审批数 |
| 密码传输安全 | HTTPS + CSRF 保护；密码哈希存储 |
| 信息泄露 | 仅精确匹配已有申请时展示状态 |

## 十三、业界参考

| 系统 | 注册方式 | 进度查询 |
| --- | --- | --- |
| GitLab Self-Managed | admin 审批制 | 登录时自动提示 pending |
| Discourse | admin 审批制 | 登录时提示「账号未激活」 |
| Keycloak | admin 审批制 | 登录时提示「账号未启用」 |
| **CronPilot（本方案）** | **admin 审批制 + 组 Scope** | **登录时自动提示** |

[文档索引](../index.html) · [Markdown](用户注册审批与忘记密码设计.md) · [索引](../index.html)

---

[← 文档索引（HTML）](../index.html) · [← 文档索引（Markdown）](../index.md)
