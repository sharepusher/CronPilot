---
description: CronPilot 项目定位、仓库边界、沟通与 Git 协作规范
alwaysApply: true
---

# CronPilot 项目总则

## 定位与仓库

- **CronPilot**：独立演进仓库（HTTP 定时回调调度台），承接 Phase A/B/C 改造与发布。
- **aniu-lee/xiaoniu_cron**：上游参考仓库，**默认不修改、不同步回写**；新功能与文档在 CronPilot 完成。
- 品牌与对外表述统一为 **CronPilot**；Release/README 中避免「小牛定时任务管理系统」等旧品牌主导表述，也避免突出「破坏性变更」标题化叙事（变更写清楚即可）。

## 技术栈约束

- Python **3.8～3.11**（勿默认 3.12+；DEC-008 / A1）。运行时栈：**Flask 2.3.3 + SA 2.0.36 + FSA 3.1.1 + gevent 23**（Phase D1）。
- 许可：**Apache-2.0**（`LICENSE`、`NOTICE`、`THIRD_PARTY_NOTICES.md`）；新增依赖需可合规分发。
- 配置：仅提交 `conf.ini.example`；**勿提交** `conf.ini`、`.venv*`、`datas/*.db`、本地日志。

## 命名与协议（绿field，不做旧名兼容）

- 回调 query：`cronpilot_log_id`、`cronpilot_sign`；验签：`get_cronpilot_sign()`。
- 进度回传：`POST /api/cron/add_log`（必传 `cronpilot_log_id`、`content`）。
- **不保留** `xiaoniu_cron_*` 等旧协议别名（除非用户明确要求兼容层）。

## 需求编号与缩写（强制）

- **权威**：`doc/需求编号与缩写规范.html`。
- **四轨**：产品优先级 P0/P1/P2 · 功能 **OPT-P\*-\*** · 依赖 **Tier \***（OPT-P2-11）· ORM/框架 **Phase \***（如 Phase D3）· 决策 **DEC-\***。
- 路线图 / RELEASE / 回复标题须写全称（如 `OPT-P1-03`、`Phase D3（OPT-P2-11）`）；禁止用裸「P1」「D1」作跨文档唯一标识。
- 「Phase A（P0）」历史发行用语 ≠ 现行 ORM Phase A Query Contract；「B1」须标明 DEC-008 或 UI。

## 工作方式（避免重复沟通）

### 设计先行（强制 · 优化与功能开发）

**凡优化（OPT / Tier / Phase / 依赖演进）与功能开发，一律：**

1. **先出设计文档** — 清晰、完整、准确（见下方「设计必备要素」）；**必须**落 `doc/design/*.html`（正式设计文档），**禁止**仅在聊天回复中给出设计而不落库。设计文档须包含所有必备要素且可被独立 Review。  
2. **经用户明确确认** — 口头/聊天中的「做吧」「完成 XX」若**未附带或未确认设计文档**，仍须先交设计再等确认；**不得**把「请完成」直接当成设计已确认。用户要求补充信息（如对比图、影响分析等）时，须补充后**再次等待明确确认**，不得在补充过程中开始实现。  
3. **确认后再执行** — 确认前 **禁止** 改业务代码、CSS/JS 静态资源、`requirements*.txt`、管理端模板、CI 行为门禁等实现向变更。

**禁止**：边设计边写代码；未确认就开干；用「先实现再补设计」交付；设计缺「分批」或「验收」仍声称可开干。

**窄例外（仍须在回复写明适用哪一条）**：

| 例外 | 条件 |
|------|------|
| 本轮已确认 | 用户对本轮设计稿明确回复确认（如「按此方案执行」） |
| 纯文档同步 | 仅 `html_docs_to_markdown` / 已交付事实对齐，无新行为 |
| 用户明示跳过 | 用户写明「跳过设计 / 直接修」的紧急热修；事后须补设计要点到 RELEASE 或设计稿 |

### 设计必备要素（缺一不可）

| 要素 | 要求 |
|------|------|
| **问题** | 现状与预期差在哪（用户/运维/工程债视角写清） |
| **根因** | 为何出现（协议 / 模型 / 依赖 / 交互 / 文档漂移等） |
| **方案** | 推荐方案；有备选时写清取舍；对齐现有风格与约束 |
| **范围** | 改哪些路径；**明确不做**什么 |
| **分批** | 如何拆步（单模块/单文件/子阶段）；每批可独立验收；禁止「九文件一次合完才测」 |
| **验收** | 可执行门禁：命令、浏览器/会话路径、断言标记；对照下方「优化验收」表 |
| **风险** | 主要回归面与回滚/降级注意点（可简） |

UI / 管理端另须补齐下表「交互契约」等行。

### Bug 修复复盘（强制 · 所有问题修复）

**触发条件（满足任一即必须复盘）**：

| 触发来源 | 示例 |
|---|---|
| 用户报告的 bug | 页面报错、功能不可用、数据错误 |
| 代码 Review / Bugbot / Security Review | 静态分析发现的缺陷 |
| **自查 / 审计发现的问题** | 文档断链、编号冲突、配置漂移、测试遗漏、CI 盲区 |
| **批量修复中的单项** | 一次修 30 处断链，仍须对根因做一次合并复盘 |

**核心原则**：**修复了问题 ≡ 必须复盘**。无论问题来源，无论严重程度，只要执行了修复动作，交付回复中就必须包含复盘要素。批量同根因修复可合并为一份复盘，但不得省略。

**复盘要素（7 项缺一不可）**：

| # | 要素 | 要求 |
|---|---|---|
| 1 | **Bug 定位** | 哪行/哪个函数/哪个路径；截图或 curl 证据 |
| 2 | **根因** | 为何出现（数据流、测试盲区、代码不对称、格式字符串错误等） |
| 3 | **测试漏洞** | 现有哪条测试「本应」能发现它但没有？根因是什么？ |
| 4 | **修复** | 最小 diff；不超出 bug 涉及范围 |
| 5 | **防护测试** | 为此 bug 新增或补强的测试（名称 + 断言内容） |
| 6 | **同类排查** | 是否存在同类问题（相同代码模式在其它路径？）；列举结论 |
| 7 | **预防方案** | **必须**提出至少 1 项可落地的预防措施（规范新增 / CI 门禁 / 代码结构调整 / 检查清单等），并明确落地位置（文件路径或流程步骤） |

**第 7 项"预防方案"是复盘的核心目的**——复盘不是为了记录历史，而是为了防止同类问题再次发生。缺少预防方案的复盘等于没有复盘。

**交付前自检**：在撰写交付总结/回复用户时，执行以下检查：
1. 本轮是否执行了任何修复动作（含文档修正、链接修复、编号更正）？
2. 若是 → 回复中是否已包含上述 **7 项**复盘要素（或合并复盘）？
3. 第 7 项"预防方案"是否有**具体措施 + 落地位置**（而非泛泛而谈）？
4. 若缺失 → **停止发送回复，先补充复盘再发送**。

**复盘质量门禁（逐项通过才算合格）**：

| # | 检查项 | 不合格标志 |
|---|---|---|
| Q1 | 预防方案是否**新增了可验证的措施**（规范条目 / CI 脚本 / 自动化测试）？ | 仅写「已有规范应执行但未执行」≡ 不合格 |
| Q2 | 预防方案能否**被第三方重现验证**（给出文件路径 + 测试命令 / grep 命令）？ | 「注意…」「加强…」等抽象描述 ≡ 不合格 |
| Q3 | 根因分析是否追到了**行为层**（为什么没执行规范、为什么测试没覆盖）？ | 只写「凭记忆」「粗心」等表象 ≡ 不合格 |
| Q4 | 预防方案与根因是否**因果对应**（方案直接解决根因，非间接相关）？ | 根因是 import 错误，方案是「加强 Code Review」≡ 不合格 |

**不合格处理**：重写复盘，直到 Q1-Q4 全部通过。不允许用形式完整掩盖内容空洞。

**AskQuestion 前置门禁（强制 · 2026-08）**：

Agent 在 invoke `AskQuestion`（或任何形式的"下一步？"提问）**之前**，必须执行以下自检：
1. 本轮回复中是否包含任何 `StrReplace`/`Write` 操作，且目的是**纠正一个不正确的状态**（代码 bug、文案错误、文档不一致、配置漂移等）？
2. 若是 → 回复中是否已包含对应的**7 项复盘要素**？
3. 若缺失 → **停止，先补充复盘，再 invoke AskQuestion**。

**程序化强制（L1+L2 Hook · 2026-08 追加）**：
- **L1**（`postToolUse` hook）：每次 `Write`/`StrReplace` 后注入上下文提醒"你刚做了修复，需要复盘"。
- **L2**（`stop` prompt hook）：Agent 结束前评估是否遗漏复盘，遗漏则自动续行要求补充。
- **L3**（TodoWrite 原子绑定）：每个修复类 Todo（如 `fix-xxx`）必须配对 `fix-xxx-postmortem` 项，两者同时 `completed` 才算任务完成。
- Hook 配置：`.cursor/hooks.json` + `.cursor/hooks/postmortem-reminder.sh`。

**禁止行为**：
- 修复后直接 AskQuestion 而跳过复盘
- 将复盘推迟到"下一轮"
- 认为"自发现的问题"不需要复盘
- 认为"文案/文档修复"不需要复盘

**根因复盘（2026-08 元复盘）**：Agent 反复违反复盘规范的结构性原因：①将复盘视为"修复的附加步骤"而非"修复的原子组成部分"；②AskQuestion 用户规则创造了"急于到达下一步"的流程锚点，导致中间步骤被跳过；③复盘触发条件在执行时被窄化为"用户报告的代码 bug"，遗漏了自发现问题和非代码修复。④ **纯文字规范被证明无法阻止反复违规**（至少 3 次被用户指出），因此升级为程序化 Hook 强制。

**禁止**：只写"已修复"而不分析测试盲区；只新增单元测试而实际故障发生在 JS/浏览器层却不说明分层差距；**修复后直接跳到总结/提交而跳过复盘**；**复盘缺少预防方案就宣称交付完成**。

### 复盘文档化（强制 · 2026-08）

**所有复盘内容必须持久化到文档**，不允许仅存在于聊天对话中。

| # | 要求 | 说明 |
|---|---|---|
| 1 | **HTML 文档** | 复盘记录到对应的设计/RFC 文档（`doc/design/*.html` 或 `doc/rfc/*.html`），或在无对应文档时新建 `doc/postmortem/YYYY-MM-功能名.html` |
| 2 | **Markdown 同步** | `python scripts/html_docs_to_markdown.py --check` 通过 |
| 3 | **RELEASE_NOTES** | 涉及用户可感知变更的复盘，需在 `RELEASE_NOTES.md` 对应版本条目中提及 |
| 4 | **规范落地** | 预防方案中涉及的规范新增/修改，必须同步更新 `.cursor/rules/` 和 `AGENTS.md` |

**禁止**：复盘只在对话中给出而不落库；规范修改了但未更新 AGENTS.md；HTML 文档更新了但 Markdown 未同步。

**测试分层认知（必须在复盘中明确）**：

| 层级 | 工具 | CronPilot 现状 | 适合拦截 |
|---|---|---|---|
| 单元 | `unittest` / `pytest` | 完善 | Python 函数逻辑 |
| 集成 | `requests.Session` | **待补充** | HTTP 全链路（含 CSRF cookie 流程） |
| E2E/Browser | Playwright / Selenium | **缺失** | JS 运行时行为（CSRF 注入、DOM 交互） |

当 bug 发生在集成或 E2E 层，必须在此层新增测试（`tests/test_*_integration.py`），而非只在单元层加断言。

### 大文件修改前结构分析（强制 · 防「作用域误判」）

**凡修改 300 行以上的 JS / Python / 模板文件，在决定插入或追加位置前必须：**

| 步骤 | 强制操作 | 禁止 |
|------|---------|------|
| **1 · 结构扫描** | 用 AST（`node -e`、`python ast`）或手动追踪 `{ }` / `def` / `class` 嵌套，确认目标插入点的 **实际作用域**（全局 / IIFE / 函数体 / 类方法） | 仅看文件首尾 20 行就决定插入位置 |
| **2 · 嵌套确认** | 对 JS：`node -c file.js` 验语法 + 用 brace depth 工具确认花括号归属。对 Python：确认 `def`/`class` 缩进层级 | 凭缩进「看上去像顶层」就插入 |
| **3 · 运行时验证** | 插入后在真实环境（浏览器 CDP / `python -c import`）中确认新代码 **在预期时机执行**，不只是「存在于文件中」 | 静态 `grep` 检查通过就宣称完成 |

**典型失误（2026-08 全局防重复提交事件）**：

| 失误 | 后果 |
|------|------|
| 只看了 `common.js` 末尾的 `})` 和 `}`，认为是 IIFE 结尾 | 守卫代码被放进 `upload_file()` 函数体，仅调用该函数时才注册 |
| 静态测试只检查 `cp-submitting` 字符串是否存在 | 假阳性 — 字符串存在但代码从未在页面加载时执行 |
| 第一次实现未做 CDP 验证就宣称完成 | 用户触发浏览器验证后才发现根本无效 |

### 表单交互变更影响分析（强制 · 防「全局守卫冲突」）

**凡修改表单提交机制（按钮 type 变更、新增模态框确认、改用 AJAX 提交等），必须检查全局 JS 对该表单的隐式依赖：**

| 步骤 | 强制操作 | 禁止 |
|------|---------|------|
| **1 · 守卫选择器审计** | `grep` 所有 `common.js` / 全局 JS 中对 `[type="submit"]`、`form:not(...)` 等选择器的监听，确认改动后这些选择器仍能匹配目标元素 | 改按钮 type 后不检查全局 JS 选择器依赖 |
| **2 · 表单提交方式确认** | 明确改动后表单使用哪种提交方式（原生 `form.submit()`、jQuery `.submit()`、AJAX），并确认该方式与全局守卫的交互结果（放行/阻止/无效） | 假设 jQuery `.submit()` 和原生 `submit()` 行为相同 |
| **3 · 端到端提交验证** | 在浏览器中走完 **填写→触发提交→服务端响应→页面跳转/反馈** 全流程，取得跳转后 URL 或 JSON 响应证据 | 只验证模态框弹出或按钮文案变化，不验证表单是否实际提交成功 |

**关键知识**：`$('form').submit()` 触发 jQuery 事件链（包括全局 `on('submit')` 守卫）；`document.querySelector('form').submit()` 原生方法**绕过** jQuery 事件链。当全局守卫可能阻止提交时，须选择正确的提交方式。

**典型失误（2026-08 注册确认模态框事件）**：

| 失误 | 后果 |
|------|------|
| 「提交申请」从 `type="submit"` 改为 `type="button"`，未检查 `common.js` 守卫 | 守卫 `!$btn.length → return false` 阻止表单提交，用户反复点击确认但表单永不提交 |
| 模态框「确认提交」用 `$('form').submit()`（jQuery）而非原生 `submit()` | jQuery 事件链触发守卫，守卫找不到 `[type="submit"]` 后 `return false` |
| 浏览器验证只验了模态框弹出和关闭，未验证「确认提交→跳转到登录页」全流程 | 假绿验收，用户实际使用时发现表单无法提交 |

### 策略变更影响分析（强制 · 防「策略断点遗漏」）

**凡引入或修改业务策略（如"停用不可恢复"、权限提升限制、字段校验规则等），必须执行以下分析，不得仅在单一入口实现策略检查：**

| 步骤 | 强制操作 | 禁止 |
|------|---------|------|
| **1 · 全路径枚举** | grep / 全局搜索策略影响的字段（如 `is_active`）在所有服务层函数中的**赋值点**，列出完整清单 | 只在"最显眼"的入口加策略，忽略间接修改路径 |
| **2 · 逐点加固** | 每个赋值点都须加入策略检查或文档说明（为何豁免）；对同一实体有多个修改入口（如 `set_user_active` + `update_user`）的，每个入口独立覆盖 | 假设"UI 不暴露 → 不需要后端防护" |
| **3 · 交叉测试** | 为每个修改入口编写独立测试用例，验证策略在该路径上生效 | 只测"主路径"就宣称策略已生效 |
| **4 · 功能一致性** | 检查同类功能（如 `create_user` vs `approve_registration`）的冲突检查逻辑是否对齐 | 新旧函数独立开发、不交叉验证 |
| **5 · 文档回溯** | grep 设计文档中策略相关关键词，逐项核实描述与实际行为一致 | 只改代码不更文档 |

**典型失误（2026-08"停用不可恢复"事件）**：

| 失误 | 后果 |
|------|------|
| 只在 `set_user_active()` 和视图层加了 `is_active=1` 拦截 | `update_user()` 通过 `is_active` 参数可绕过策略 |
| `create_user()` 和 `submit_registration()` 的用户名冲突检查逻辑不一致 | 管理员手动创建同名用户被拒，但注册审批可通过 |
| admin 角色注册变更只改了代码和测试 | 设计文档仍写着"admin 不能通过注册申请" |

### 浏览器端 DOM / CSS / JS 修改（强制 · 防「用户反复空验收」）

凡写 CSS 选择器、JavaScript 注入（包括 iframe 注入）或第三方 UI 组件定制，**必须遵守「DOM 优先」规程**：

| 步骤 | 强制操作 | 禁止 |
|---|---|---|
| **1 · 查 DOM 再写选择器** | 先用 CDP `Runtime.evaluate` 或 `browser_cdp` 取真实 HTML（`innerHTML`、`querySelectorAll` 等），确认目标元素的**实际 class/标签**后再写代码 | 凭记忆或经验假设第三方库的类名（如 `.no-parameters`）直接写选择器 |
| **2 · 干跑验证（dry-run）** | 用 CDP 在当前 DOM 上模拟 `hideEmptyParams(document.body)` 等函数，返回 `wouldHide: true/false`，确认逻辑正确 | 只写代码不跑 dry-run 就宣称逻辑正确 |
| **3 · 端对端 CDP 断言** | restart 后导航到目标页，用 CDP 模拟用户操作（click → 等待 → 查 `style.display`），**取得 `display:none` 的断言证据** | 没有 CDP 断言证据就说「请你自行验证」 |
| **4 · 截图** | 取得 `browser_take_screenshot` 截图附在交付回复中 | 仅文字描述，不附截图 |

**iframe 内注入专项**：
- iframe 内的第三方 JS 框架（如 React-based Swagger UI）使用合成事件，标准 `MouseEvent.dispatchEvent` 无法触发。**不要试图用 CDP click 展开 React 组件**；应改用对已展开组件的静态 DOM 检查，或先在独立页面（非 iframe）中展开再取 DOM。
- 注入脚本失效的常见原因：选择器不存在、`onload` 时机过早、iframe `contentDocument` 为 `null`。每次应逐项 CDP 排查，不得连续换方案而不诊断失效原因。

**典型失误（2026-07 API doc Parameters 隐藏事件）**：

| 失误 | 教训 |
|---|---|
| 用了不存在的 `.no-parameters` class | 应先 `innerHTML` 取 DOM，再写选择器 |
| 3 次改代码都让用户验证，未先 CDP 自证 | 自己先 dry-run，green 才告知用户 |
| 隐藏了 `.opblock-section` 父级，把 Request body 也藏了 | 应先取 DOM 层级图再决定隐藏哪一层 |

### 交付闭环（强制顺序）

任一步未通过则 **修复 → 重新验证 → 同步更新相关文档**，再进入下一步或 commit。

| 步骤 | 动作 | 门禁 |
|------|------|------|
| 1 · **设计** | 按「设计必备要素」出稿（`doc/*.html` 或等价结构化回复） | **用户确认前不写实现代码** |
| 2 · **实现** | 按已确认分批最小 diff；改 `app/` 前先读 `cron_validator` / `cron_service` / 现有 `views` | 不得超出已确认范围 |
| 2.5 · **复盘**（所有修复） | **凡本轮执行了任何修复动作**（bug 修复、Review 发现的问题修复、文档修正、编号更正等），**必须**在回复中写出 `## 复盘：<问题名称>` + 7 要素（Bug 定位 / 根因 / 测试漏洞 / 修复 / 防护测试 / 同类排查 / **预防方案**）。**复盘必须在同一条回复中**，不得"下一轮补"。**在给出 AskQuestion 选项之前必须自检：本轮是否有修复？有则先复盘再选项。** | **缺少复盘或预防方案，视为交付不完整，不得进入验证步骤** |
| 3 · **验证** | 按已确认验收项 + 下方「优化验收」表 | **未通过不得更新「已交付」文档、不得 commit** |
| 4 · **可验证本地环境** | 见下「交付后可验证本地环境」：restart → 给出 URL/登录/步骤/断言 → Agent 自证 → **默认保持服务运行** | **未给出可访问入口与断言证据，不得宣称交付完成** |
| 5 · **文档** | `RELEASE_NOTES.md` + 相关 `doc/*.html`（RFC、PRD、部署、UI 设计等）→ `html_docs_to_markdown.py --check`；**打 tag / Release 前**须同步 `doc/交付状态与路线图.html`（已交付 vs 未完成总览） | 与代码变更 **同一轮** 完成 |
| 6 · **提交** | 仅用户明确要求时 `commit`/`push`；**代码 + 文档同一 commit**（除非用户要求拆 PR） | 工作区干净 |

**禁止**：只改代码不更文档就宣称交付；只更文档不跑验证；验证通过但文档/commit 留到下一轮；**单测绿了却不给用户可访问的本地/Docker 验收入口**。

1. **最小 diff**：只改与任务相关的文件；不顺手重构、不扩 scope。
2. **Git**：不擅自 force push、不 amend 已推送提交。
3. **删除纪律**：删除 `doc/_pending_sync/` 或其它仓库文件时，须在回复/提交说明与 `doc/_pending_sync/已合并补丁记录.md` 中写明 **完整相对路径** 与 **充分删除原因**（附主文档对照证据），避免误删。
4. **部署说明**：裸机优先写 `README` 部署节 + `doc/ops/非Docker部署指南.html`（及生成 MD）；管理端与文档同端口 `/docs/`。

### UI / 功能设计确认（在「设计必备要素」之上追加）

| 项 | 内容 |
|----|------|
| 问题 | 用户看到什么、与预期差在哪 |
| 根因 | 数据模型 / 协议 / 交互误导 / 样式不一致等 |
| 方案 | 推荐方案 + 备选；须对齐现有 simpleboot 表单风格（`control-group` + 行内灰字说明） |
| 交互契约 | **Ajax 表单**（`js-ajax-form` + `js-ajax-submit` + JSON `url` 跳转）/ **普通跳转** / 两者兼有 — 须写明；新建表单以 `cron_add.html` / `cron_retire.html` 为对照样板 |
| **选项排序** | 下拉/多选中的高权限/全局操作项（如「全部」「admin」）**必须**放在末尾，降低误选概率 |
| 范围 | 涉及模板、API、文档 |
| 分批 | 页面/接口/权限分步；每步可浏览器验收 |
| 验收 | 浏览器路径 + Docker/本地步骤（见下「管理端表单」最低验收） |

未确认前：**还原或不要提交** 试探性 UI / 功能 diff。

### 优化验收（验证通过后再写文档）

| 变更类型 | 最低验收 |
|----------|----------|
| 后端 / API / 服务层 | `bash scripts/cronpilot.sh test` + `bash scripts/verify_golden_path.sh` |
| **管理端表单 / 导航 / 可见交互** | 上表 **外加**：① 静态门禁 `tests.test_ajax_form_guard`；② **浏览器或等价会话**走通目标路径；③ **必须先 `cronpilot.sh restart`（见下「本地进程与热更新」）再验收 HTML**，禁止只让用户「刷新浏览器」；④ **涉及多组/多角色**的功能，必须以至少一个**受限用户**（非 bypass）登录验证显示完整性 |
| 依赖升级（Tier N） / `requirements*.txt` 侧车补丁 | `cronpilot.sh test` + **`docker compose up --build -d` 后容器内确认 pin**（如 `pip show requests urllib3`）+ **`smoke_http_suite` 或 `verify_all.sh --docker-only --with-compose`**；**通过前不得 commit** |
| 部署 / 启动脚本 | `verify_install_flow.sh` 或 `verify_all.sh` 中对应节 |
| 仅文档 | `html_docs_to_markdown.py --check` + `check_pending_sync.sh`（无代码则可不跑服务） |

**禁止**：无提交按钮的空壳 `js-ajax-form`（历史删除流勿再套）；缺 `js-ajax-submit` 会导致浏览器整页 POST 落在裸 JSON。

### 交付后可验证本地环境（强制 · 防「测过但用户无法验收」）

**目标**：每一轮优化/功能在对外说「已完成 / 请验收」时，用户必须能**立刻**在本地（或约定的 Docker）打开并按步骤复现，而不是只看到「单测通过」。

**适用**：凡改动 `app/`、`scripts/` 启动行为、管理端模板、配置门禁、或用户可感知行为的优化；纯文档同步可豁免「起服务」，但仍须给出 `--check` 证据。

**强制清单（回复用户时须写全）**：

| # | 要求 | 说明 |
|---|------|------|
| 1 | **进程就绪** | `bash scripts/cronpilot.sh restart --daemon`（或 Docker：`compose up/restart`）；`lsof -nP -iTCP:5001 -sTCP:LISTEN`（或 `:5860`）确认新 PID |
| 2 | **访问入口** | 写明 URL（默认 `http://127.0.0.1:5001/`）、登录账号来源（如种子 `admin` / `conf.ini` 的 `login_pwd`） |
| 3 | **验收步骤** | 3～8 步可执行路径（curl 或浏览器）；每步带**期望结果**（状态码、文案、JSON 字段、文件是否生成等） |
| 4 | **Agent 自证** | 本轮至少执行并摘录一条关键断言证据（命令输出摘要）；禁止只说「请你自行验证」 |
| 5 | **保持可用** | 交付回复后**默认保持**本地/容器服务运行；勿擅自 `stop` / `compose down`（用户明确要求关闭或下一任务需独占端口除外） |
| 6 | **无 UI 变更** | 仍须起服务并给出与本轮相关的可执行断言（如：弱 `SECRET_KEY` 拒启、锁单测+集群配置说明）；不可用「无界面」跳过整节 |

**禁止话术**：「单测已通过，功能可用」——若未附 URL + 步骤 + 自证证据，视为未完成交付闭环第 4 步。

### 大变更浏览器关键路径验证（强制 · 2026-07 追加）

**适用**：凡涉及 RBAC 鉴权、Scope 过滤、用户管理、权限矩阵变更、导航/页面可见性变更等**大变更**（影响 ≥ 3 个文件或涉及权限/可见性语义），必须在浏览器中主动验证关键路径，不得仅靠单元测试 + curl 宣称交付。

**强制清单**：

| # | 要求 | 说明 |
|---|---|---|
| 1 | **restart 后浏览器登录** | 用目标角色账号（如按组管理员）实际登录，不仅用种子 admin |
| 2 | **正向路径** | 验证目标角色能看到/操作其 Scope 内的资源（任务、用户、组等） |
| 3 | **反向路径** | 验证目标角色**不能**看到/操作 Scope 外的资源（导航不展示、列表不包含、操作被拦截） |
| 4 | **截图留证** | 用 `browser_take_screenshot` 至少截取 1 张关键页面截图，附在交付回复中 |
| 5 | **边界用例** | 角色切换（如种子 vs 管理员 admin）、空组、全局选项等边界场景 |

**禁止**：仅用 curl 看 HTTP 200 就宣称"浏览器验收通过"；仅在种子 admin 下验证而未测试受限角色。

### Redesign 侧边栏角色权限回归（强制 · 2026-08 追加）

**适用**：凡修改 `app/templates/redesign/_sidebar.html`、`app/rbac/policy.py`、`app/rbac/context.py` 或权限字符串定义。

**回归门禁**：`python -m unittest tests.test_redesign_sidebar -v`（12 个用例）

### Redesign Mockup 逐节对照（强制 · 2026-08 追加）

**适用**：实现 `doc/design/CronPilot-2026-redesign-mockup.html` 中已定义的任何页面/组件。

**强制清单**：

| 步骤 | 强制操作 | 禁止 |
|---|---|---|
| **1 · Read Mockup 源码** | 先 `Read` Mockup 中对应 `view-*` 区块的**完整 HTML**（含 CSS class、列数、组件层级） | 凭记忆或对 Mockup 的"印象"开始编码 |
| **2 · 列出结构清单** | 在开始实现前，显式列出：Stats 指标名与顺序、表格列数与列名、按钮类型(icon/text)、特殊面板(Exception Panel 等)、行样式(高亮/淡化) | 直接开始写模板 |
| **3 · 实现后结构断言** | `curl + grep` 验证渲染 HTML 中存在 Mockup 规定的关键 CSS class（如 `exc-panel`、`row-acts`、`hf-stats`） | 仅看"页面能打开"就宣称对齐 |
| **4 · 截图逐区域对照** | 截图并逐区域（Stats/Panel/Filters/Table/Pagination）与 Mockup 对比 | 只看整体"差不多"就交付 |

**典型失误（2026-08 Phase 2 首次实现事件）**：

| 失误 | 后果 |
|---|---|
| 未 Read Mockup 源码即开始实现 | Stats 指标名和顺序错误；Exception Panel 完全缺失 |
| 将 Mockup 的 7 列表格凭印象简化为 5 列 | 缺失健康度、下次执行两个关键列 |
| Icon-only 按钮实现为文字按钮 | 视觉与 Mockup 严重不一致 |
| 首次验证只确认"数据显示正确" | 结构偏离直到用户全面 Review 才暴露 |

**覆盖矩阵**：

| 角色 | 预期导航项数 | 反向 403 拦截验证 |
|------|:-----------:|----------|
| Seed Admin | 12 | `/rbac/users` → 200 |
| Biz Admin | 12 | 同上 |
| Operator | 7 | `/rbac/users` → 403；`/cron_add` → 200 |
| Viewer | 6 | `/cron_add` → 403；`/rbac/users` → 403；`/operation_log_list` → 403 |

**权限字符串对照**（7 个）：`cron:read` · `cron:write` · `cron:retire` · `log:read` · `operation:read` · `user:manage` · `audit:read`

**修改侧边栏后交付闭环**：① 单测通过 → ② restart + 浏览器 4 角色截图 → ③ 更新本文件角色矩阵（如导航项数变化）

**Scope 隔离回归测试（强制 · 2026-08）**：凡修改 `app/rbac/scope.py`、`app/rbac/policy.py`（`user_bypasses_scope`）、`app/main/views.py`（`build_scope_filter_clause` 调用）时，必须运行 `python -m unittest tests.test_rbac_scope.TestScopeIntegration -v` 确认 Scope 隔离完整。覆盖场景：单组 operator 不可见其他组；双组 operator 可见两组；Biz Admin 有组时受限；Seed Admin 绕过全部。

### 本地进程与热更新（强制 · 防「刷新无效」）

**事实**：`scripts/start_local_full.sh` 以 `app.run(..., debug=False, use_reloader=False)` 常驻。Flask `DEBUG=False` 时 **Jinja 默认不按 mtime 重载模板**；Python 模块亦只在进程启动时 import 一次。因此改 `app/**/*.py` 或 `app/templates/**` 后，**浏览器刷新不能加载新代码**。

**根因复盘（2026-07 顶栏/角色色事故）**：

| 失误 | 后果 |
|------|------|
| Agent 改完模板/文案后只提示用户「刷新」 | 用户连到旧进程，以为改动未交付 |
| `stop`/`kill` 在沙箱或端口残留失败后仍 `curl` 验收 | curl 命中旧 PID，验收假绿或假红 |
| `start \| tail` 阻塞或未 `--force` 启动失败却未察觉 | 旧进程继续服务 |
| 与 Docker `:5860` / 本地 `:5001` 混淆 | 改了裸机、看的是容器（或反过来） |

**强制策略（Agent 与人工同等遵守）**：

1. **任一**会影响管理端 HTML/行为的改动（`app/templates/`、`app/rbac/`、`app/main/views.py`、静态 CSS 注入进 base 等）→ 对外宣称「请看效果」**之前**必须：
   ```bash
   bash scripts/cronpilot.sh restart --daemon
   # 确认新进程在听：
   lsof -nP -iTCP:5001 -sTCP:LISTEN
   ```
   若 `stop`/`kill` 因权限失败，**申请 `all` 权限重跑**，不得带着占用端口继续验收。
2. **验收证据**：登录会话下 `curl`/`浏览器` 断言 **本轮新增标记**（如新 class、新中文/英文文案）；**禁止**仅凭单测通过就让用户刷新。**重启后须先 `curl -s http://127.0.0.1:5001/rbac/login -w "%{http_code}"` 确认 200**，排除 DB 表缺失等全局 500，再进入功能验收。
3. 回复用户时写清端口：本地默认 **`http://127.0.0.1:5001/`**；Docker 默认 **`:5860`**。两者进程独立，改一侧须在对应侧 restart / `compose restart`。
4. **禁止话术**：「刷新即可看到」——除非已完成步骤 1–2 且 curl 已见新标记。正确话术：「已 `restart`，请硬刷新（Cmd+Shift+R）`http://127.0.0.1:5001/`」。
5. Docker 路径同理：改挂载/镜像内代码后须 `docker compose restart`（或 rebuild），**不要**只刷新浏览器。

大批量改写（如查询 backlog）建议 **单文件（或单模块）→ 验收 → 文档**，避免多文件改完才测。

### 优化后文档同步清单

按变更勾选相关落点（详 `.cursor/rules/cronpilot-documentation.mdc`）：

- [ ] `RELEASE_NOTES.md` + `doc/RELEASE_NOTES.html`
- [ ] 依赖路线：`doc/deps/依赖升级RFC.html`
- [ ] 架构/方案/PRD：对应 `doc/*.html`
- [ ] 部署/验收：`README`、`doc/ops/非Docker部署指南.html`、`doc/qa/P0测试用例与验收手册.html`（若影响验收）
- [ ] `AGENTS.md` / `.cursor/rules/`（若流程或命令变更）
- [ ] `python scripts/html_docs_to_markdown.py --check` 通过
- [ ] 若删除 `doc/_pending_sync/*`：`已合并补丁记录.md` 已记完整路径与原因

### 设计变更后文档即时同步（追加 2026-08）

**触发条件**：在同一 OPT/功能的实现过程中发生**设计变更**（如 rollback → 重新设计、删除/新增功能点、模型结构调整等）。

**强制规则**：
- 设计变更确认后，必须**立即**更新 `RELEASE_NOTES.md` 草稿中对应章节的描述，使其反映**当前实际设计**，不可等最终 commit 时才补。
- 特别关注：数量（测试数）、模型关系（一对一/多对多）、API 字段名（`group_id` vs `group_ids`）、功能可用性（是否已移除）。
- **Review 交叉验证**：全面 review 时须逐条对比 RELEASE_NOTES 描述与代码实际行为是否一致。

**根因复盘**：2026-08 OPT-P1-11 三次设计变更（多组→单组、合并→移除、全局标签→组隔离），RELEASE_NOTES 仅在首次实现时写入，后续变更未触发同步，导致 4 处事实性错误。

### 删除功能全仓库引用搜索（追加 2026-08）

**触发条件**：删除/移除任何功能、路由、函数、模板时。

**强制规则**：
- 必须在全仓库搜索被删功能的**中文名称**（如"合并"）、**英文名称**（如"merge"）、**函数名**（如 `merge_tags`），确认无残留引用（含注释、错误提示文案、文档描述）。
- 残留引用须一并修正或明确注释为历史引用。

**根因复盘**：2026-08 移除标签合并功能时，`rename_tag` 的错误提示和文件头注释仍引用"合并功能"。若执行 `grep -r "合并" app/` 可立即发现。

### 禁止在中间整合文档中重复源文档数值（追加 2026-08）

**触发条件**：凡创建或编辑「整合型文档」（汇总/索引/手册等引用多个源文档的文档）。

**强制规则**：
- **禁止**将源文档中的精确数值（色值、字号、间距、尺寸、CSS 属性值等）复制到整合文档中。
- 整合文档只允许包含：① 架构决策（定性描述）② 源文档定位索引（文件名 + 章节号 + 行号） ③ 流程/规程描述。
- 需要引用具体数值时，使用 **链接 + 位置指示**（如「见视觉规格书 §1.3 行 86–110」），不得内联写出数值。
- **实现代码**在编写时必须 `Read` 源文档获取数值，禁止从整合文档或记忆中获取。

**根因复盘**：2026-08 统一执行手册 v1 从视觉规格书/Mockup 抄写数值时引入 15 处偏差。Agent 在"综合写入"步骤中依赖记忆而非机械复制。改为 v2 纯索引架构后根除此问题。详见 `doc/postmortem/2026-08-统一手册数值重复偏离.html`。

### GitHub Release 发布文案规范（追加 2026-07）

- 面向 GitHub 的发布说明（Release title / Release notes）统一使用**专业英文**，保持客观、简洁、可执行（包含影响范围、升级/回滚要点、验证摘要）。
- 禁止口语化或中英混杂标题（例如“已修好/快看”）；禁止仅贴原始日志而不做结构化说明。
- 发布文案至少覆盖：`What changed`、`Why`、`Validation`、`Compatibility/Risk` 四段。
- 若本轮涉及发布流程调整，需同步更新 `RELEASE_NOTES.md` 与 `doc/RELEASE_NOTES.html`，避免“代码已改但发布文案规范未落库”。

## 路线图优先级（未明确要求不越级实现）

| 阶段 | 范围 | 状态 |
|------|------|------|
| Phase A (P0) | SQL ORM、密码哈希、SSRF、JSON 契约、cron 校验统一 | 已交付，回归时勿回退 |
| Phase B (P1) | 执行 status、失败规则、详情页、立即执行等 | 文档/PRD 为主，实现需用户确认 |
| Phase C (P2) | SSE、图表、OAuth 等 | 同上 |
| 依赖升级 (OPT-P2-11) | Tier 0 ✓ → Tier 1 ✓ SA 1.4 → **Tier 2 ✓** gevent/gunicorn/APS/Python → **Tier 3 前置 ✓**（去 records）→ Tier 3a/4 | 权威路线 `doc/deps/依赖升级RFC.html`；RBAC 可与 Tier 1 后并行 |

## 测试

- 单元测试：`python -m unittest tests.test_p0_phase_a tests.test_cronpilot_sign -v` 或 `bash scripts/cronpilot.sh test`
- 集成冒烟：`bash scripts/verify_golden_path.sh`；全量 `bash scripts/verify_all.sh`（Docker 用 `--docker-only --with-compose`）
- 用例应覆盖真实行为（SSRF、密码、校验、JSON、签名），不写无意义断言。
- CI 已配置：**Unit tests**、**Docs HTML ↔ Markdown sync**、**Hardcoded color audit**、**Frontend dist freshness**；改 `doc/*.html` 后须 `--check` 通过。
- **顺序**：见上文「交付闭环」5 步。Docker 验收后若需用户手工验证，**保持容器运行**，给出 URL/密码，勿擅自 `docker compose down`。

### 测试数据库隔离（强制 · 2026-08 system err 事故教训）

**事故根因**：测试代码 `from manage import app, db` 将 `db` 绑定到开发数据库 `datas/job_log.sqlite`，`tearDown` 中 `db.drop_all()` 删除了全部 11 张业务表，导致服务启动后全站 500。测试套件本身因各自独立的内存数据库仍全部通过，掩盖了对磁盘数据库的破坏。

**强制规范**：

| 规则 | 要求 |
|---|---|
| **禁止 `from manage import`** | 测试文件中**严禁** `from manage import app` 或 `from manage import db`。`manage.py` 在模块级别执行 `create_app('development')`，导入即绑定开发数据库 |
| **必须使用内存数据库** | 所有测试必须使用 `sqlite:///:memory:` 创建独立 Flask app；参考 `_make_app()` 工厂模式（`test_registration.py`） |
| **`drop_all()` 仅在内存上下文** | `tearDown` 中的 `db.drop_all()` 必须在内存 app 的 `app_context()` 中调用 |
| **测试后验证数据库完整性** | 改动测试文件后，须在测试完成后执行 `sqlite3 datas/job_log.sqlite ".tables"` 确认表未被破坏 |
| **交付前端到端验证** | `cronpilot.sh test` 通过后，必须 `cronpilot.sh restart` → 浏览器登录（POST，非仅 GET）→ 确认主页正常加载 |

**自检命令**：
```bash
# 测试文件中不应出现 from manage import
grep -r "from manage import" tests/ && echo "FAIL: 测试禁止 from manage import" && exit 1
```

## 颜色规范（强制）

- **禁止**在 `app/templates/**/*.html` 和 `frontend/src/**/*.vue` 中硬编码十六进制颜色（`#xxxxxx`）。
- 所有颜色必须使用 `app/static/css/console-theme.css` 中定义的 CSS 变量（`var(--cp-*)`）。
- 新增颜色需求：先在 `console-theme.css` 的 `:root` 中定义语义变量（`--cp-新语义名: #色值;`），再在模板/组件中引用 `var(--cp-新语义名)`。
- CI 门禁：`python scripts/audit_hardcoded_colors.py --check` 会阻断含硬编码颜色的 PR。
- Vue 组件改动后须 `cd frontend && npm run build`，构建产物须随源码一起提交（CI `frontend-build.yml` 检查）。
- 审计工具：`python scripts/audit_hardcoded_colors.py`（完整报告）/ `--mapping`（映射表）/ `--csv`（导出）。

## 表单防重复提交（强制）

- 所有 `method="post"` 的 `<form>` 必须有防重复提交保护。
- **`js-ajax-form`**：已由 `common.js` 的 `btn.data("loading")` 守卫保护，无需额外处理。
- **普通 POST 表单**（非 `js-ajax-form`）：由 `common.js` 底部的全局守卫自动保护——提交时禁用 submit 按钮 + 文案追加「中…」，3 秒后自动恢复。
- **GET 搜索表单**：幂等操作，无需防重复提交。
- **新增表单时**：确认使用了 `js-ajax-form` + `js-ajax-submit`（推荐）或确认全局守卫可覆盖；**禁止**新增无保护的 POST 表单。
- **独立页面**（如 `register.html`、`login.html` 不继承 `admin_base.html`）：须确认引入了 `common.js`（含全局守卫），或自行实现等效保护。

## 版本一致性（强制）

- CI 门禁：`python scripts/check_version_consistency.py --check` 校验所有 `vX.Y.Z` git tag 在 README 版本表、`doc/交付状态与路线图.html` 已发布版本表、`RELEASE_NOTES.md` 中均有对应条目。
- **发版必须同步更新**：① README 版本表行 ② README 概述行版本号 ③ 路线图版本行 ④ RELEASE_NOTES 章节 ⑤ 路线图中已交付 OPT/RFC 状态。
- **编号分配前**须 `grep 'OPT-P[0-2]-' doc/交付状态与路线图.html` 确认编号未被占用。
