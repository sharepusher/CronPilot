# 表单字段级错误提示设计 — CronPilot

> HTML 版：[表单字段级错误提示设计.html](表单字段级错误提示设计.html) · [文档索引](../index.html) · [索引 Markdown](../index.md)

# 表单字段级错误提示设计

版本 rev.4 · 2026-09-01 · 编号 OPT-P1-14

## 1. 问题

### 1.1 用户痛点

当前 Redesign 表单（以任务新建/编辑页为主）的验证错误反馈采用**全局 Toast + 字段高亮**模式：

- **错误文案**：CpToast 固定在视口右上角（`position: fixed; top: 16px; right: 16px`），4 秒后自动消失
- **字段高亮**：出错字段添加 `tf-field-error`（红色边框 + 标签变红）或 `tf-section-error`（仅标签变红）
- **滚动定位**：`scrollIntoView({ behavior: 'smooth', block: 'center' })`

**核心缺陷**：错误文案（"任务名称不能为空"）出现在视口右上角，出错字段在表单中部或底部。用户必须「向上看 Toast → 记住错误内容 → 向下看高亮字段 → 理解并修正」，视觉路径断裂。

### 1.2 格式塔心理学：接近性原则（Law of Proximity）

「空间上相近的元素被感知为同一组」——错误说明文案应与引发错误的输入控件紧密相邻，而非分散在页面不同区域。当前设计违反了这一基本原则。

### 1.3 现状快照

| 层级 | 机制 | 文件 | 覆盖 |
| --- | --- | --- | --- |
| 1. 全局 Toast | `CpToast.error(errmsg)` | common-redesign.js:70 | 所有 js-ajax-form |
| 2. 字段高亮 | `tf-field-error` / `tf-section-error` | common-redesign.js:76-82 | 仅有 data-field 的表单 |
| 3. 滚动定位 | `scrollIntoView` | common-redesign.js:83 | 同上 |
| 4. 模态框内联 | `.tg-err` / `.cp-modal-err` | tags.html / dashboard.html | 仅模态框场景 |

**缺失**：表单页面（非模态框）没有**字段旁内联错误文案**机制。

## 2. 根因

1. **历史架构**：v1 使用 jQuery `alert()` 报错，Redesign 升级为 Toast 是进步但未走完最后一步
2. **通用框架缺少字段级文案规范**：`common-redesign.js` 的 AJAX handler 只处理 `field` 键用于高亮，不处理消息文案的就近显示
3. **后端契约未区分**：`web_api_return(code=1, msg=err, data={'field': field})` 中 `msg` 同时用于 Toast 文案和（缺失的）字段级文案

## 3. 业界模式对比

| 模式 | 代表 | 机制 | 优点 | 缺点 | 适合场景 |
| --- | --- | --- | --- | --- | --- |
| A · 字段下方内联文案 | Material Design、Ant Design、Bootstrap 5、GitHub | 在 input 下方显示红色小字 | 空间关联最强；持久可见；无遮挡；业界最通用 | 可能导致布局偏移（可通过预留空间解决） | **表单页**（CronPilot 主战场） |
| B · 全局 Toast | Slack、部分后台管理系统 | 固定位置弹出通知 | 实现简单；不影响布局 | 视觉路径断裂；自动消失后信息丢失 | 非表单操作（删除确认、权限变更等） |
| C · Summary + Inline 双层 | W3C WCAG 推荐、gov.uk | 页面顶部错误摘要 + 字段旁内联 | 最佳无障碍；适合超长表单 | 实现复杂；CronPilot 表单字段数 ≤15，性价比低 | 超长表单 / 政务/无障碍要求高 |
| D · Tooltip/Popover | 部分 jQuery 插件 | 字段旁浮动气泡 | 不影响布局 | 可被遮挡；移动端体验差；不够持久 | 不推荐 |

## 4. 方案：A · 字段下方内联文案（推荐）

### 4.1 设计原则

1. **就近显示**：错误文案紧跟在出错字段的 input/select 下方，替代 hint 位置
2. **保留 Toast 作为降级**：当后端未返回 `field` 键时（通用错误），仍用 Toast 全局提示
3. **互斥显示**：有 `field` 时 → 内联文案 + 高亮 + 滚动，**不再 Toast**；无 `field` 时 → 仅 Toast
4. **自动清除**：用户修改出错字段内容时，自动清除该字段的错误状态和文案，恢复原始 hint
5. **Section 处理**：对 `cron_div` 等区段，错误文案使用 `insertAfter(label)` 插入在区段标签与子字段网格之间
6. **无障碍**：`.tf-error-msg` 添加 `role="alert"` 属性，使屏幕阅读器自动朗读错误信息（WCAG 最佳实践，参考 React Hook Form / Formik）
7. **禁用原生校验**：凡启用 `.tf-error-msg` 内联文案的表单，必须添加 `novalidate` 属性。JS 中 `checkValidity/reportValidity` 对 `novalidate` 表单自动跳过。原因：浏览器原生校验气泡（如「请填写此字段」）样式不可控，与自定义内联文案冲突

### 4.1.1 Hint 隐藏与恢复策略

错误文案与 hint 不能同时显示（视觉混乱）。策略：

1. **显示错误时**：找到 `.tf-hint` 元素，`style.display = 'none'` 隐藏，同时在该位置插入 `.tf-error-msg`
2. **清除错误时**：移除 `.tf-error-msg` DOM 元素，恢复 `.tf-hint` 的 `display`
3. **Section 容器**（如 `cron_div`）：无直属 `.tf-hint`，`.tf-error-msg` 直接 `insertAfter` 于 `.tf-label` 后

### 4.1.2 Section 插入位置

对于 `#cron_div` 等区段容器，DOM 结构为：

```
<div id="cron_div" data-field="cron_div">
  <label class="tf-label">Cron 表达式 *</label>
  <!-- ← .tf-error-msg 插入此处 -->
  <div class="tf-cron-grid">...子字段...</div>
</div>
```

JS 实现须使用 `$el.find('> .tf-label').after(errorMsg)`，而非简单 `append`。

### 4.2 交互流程

```
用户提交表单
  └→ 后端返回 { errcode: 1, errmsg: "Cron 表达式不能全部为空", data: { field: "cron_div" } }
      └→ JS 收到响应
          ├→ 有 field key?
          │   ├→ YES: ① 清除所有旧错误状态
          │   │       ② 找到 [data-field] 或 #id 元素
          │   │       ③ 添加 tf-field-error / tf-section-error
          │   │       ④ 在目标元素内插入/更新 .tf-error-msg 内联文案
          │   │       ⑤ scrollIntoView
          │   │       ⑥ 不弹 Toast
          │   └→ NO:  ① CpToast.error(errmsg)（保持现有行为）
          └→ 用户修改该字段 → input 事件 → 清除该字段的 error 状态 + 移除 .tf-error-msg
```

### 4.3 对比 Demo

#### A · 当前方案（全局 Toast + 高亮）

新建定时任务

⚠ Cron 表达式不能全部为空

任务名称

Cron 表达式 \*

日（号）

小时

分钟

← 标签变红了，但错误描述在右上角 Toast 里，空间分离

↓ 优化后 ↓

#### B · 推荐方案（字段旁内联文案）

新建定时任务

任务名称

Cron 表达式 \*

Cron 表达式不能全部为空

日（号）

小时

分钟

✓ 错误描述紧跟标签下方，视觉路径连续

#### C · 单字段错误对比（如：任务名称为空）

单字段错误 — 推荐方案效果

任务名称 \*

任务名称不能为空

回调 URL

完整的 HTTP/HTTPS URL

说明：正常字段保留 hint，出错字段 hint 被错误文案替换

### 4.4 CSS 规格

| 属性 | 值 | 来源 |
| --- | --- | --- |
| 文案容器 class | `.tf-error-msg` | 新增 |
| 字号 | `12px` | 与 `.tf-hint` 一致 |
| 颜色 | `var(--cp-danger)` | 已有 token |
| 间距 | `margin-top: 4px` | 与 `.tf-hint` 一致 |
| 图标 | `⚠` 前缀（CSS `::before`） | 增强辨识度 |
| 过渡 | `fadeIn 0.15s ease` | 柔和出现，不突兀 |
| 层级 | 文档流内（非 absolute） | 不遮挡其他元素 |
| ARIA | `role="alert"` | WCAG 无障碍：屏幕阅读器自动朗读 |

### 4.5 HTML 结构

JS 动态插入，无需修改模板：

```
<!-- 单字段错误 -->
<div class="tf-field tf-field-error" data-field="task_name">
  <label class="tf-label">任务名称 *</label>
  <input class="tf-input" name="task_name">
  <div class="tf-error-msg">任务名称不能为空</div>  <!-- JS 动态插入 -->
  <span class="tf-hint" style="display:none">...</span>  <!-- hint 被隐藏 -->
</div>

<!-- 区段错误（cron_div） -->
<div id="cron_div" data-field="cron_div" class="tf-section-error">
  <label class="tf-label">Cron 表达式 *</label>
  <div class="tf-error-msg">Cron 表达式不能全部为空</div>  <!-- JS 动态插入在 label 后 -->
  <div class="tf-cron-grid">...</div>
</div>
```

## 5. 范围

### 5.1 改动文件

| 文件 | 改动内容 | 影响 |
| --- | --- | --- |
| `app/static/js/common-redesign.js` | AJAX 错误处理：有 field → 内联文案 + 不 Toast；无 field → Toast 降级 | 所有 js-ajax-form 表单 |
| `app/static/css/redesign-pages.css` | 新增 `.tf-error-msg` 样式 + fadeIn 动画 | 任务表单页 |
| `app/templates/redesign/task_form.html` | input 事件清除错误（已有基础，扩展为同时移除 `.tf-error-msg`） | 任务表单页 |

### 5.2 不做什么

- **不修改后端 API 契约**：`web_api_return(code, msg, data={'field': ...})` 保持不变
- **不改模态框错误显示**：模态框（tags、retire、deactivate）已有各自的 `.tg-err` / `.cp-modal-err` 机制，本轮不统一
- **不新增模板 DOM**：`.tf-error-msg` 由 JS 动态创建/销毁，不需要在模板中预埋
- **不改 HTML5 原生校验**：`checkValidity() / reportValidity()` 保持，内联文案仅处理 AJAX 服务端校验

## 6. 分批

| 批次 | 内容 | 可独立验收 |
| --- | --- | --- |
| S1 | CSS：`.tf-error-msg` 样式 + fadeIn 动画 | ✓ 手动添加 class 可见效果 |
| S2 | JS：`common-redesign.js` AJAX handler 改造 — 有 field 时插入内联文案 + 不弹 Toast | ✓ 提交空表单 → cron/名称错误显示在字段旁 |
| S3 | JS：input 事件清除 — 用户修改字段时自动移除 `.tf-error-msg` + 恢复 hint | ✓ 修改字段内容 → 红色消失 + hint 恢复 |
| S4 | 验证 + 文档同步 | ✓ restart → 全流程验收 |

## 7. 验收

### 7.1 功能验收

| # | 场景 | 期望 |
| --- | --- | --- |
| 1 | 新建任务 → 不填任何字段 → 提交 | 任务名称字段下方出现红色内联文案；无 Toast |
| 2 | 修正名称 → 不填 cron → 提交 | cron 区段标签变红 + 标签下方出现内联文案；无 Toast |
| 3 | 填入非法 cron → 提交 | cron 区段内联文案显示校验错误；子字段不全部变红 |
| 4 | 修改出错字段内容 | 内联文案消失 + hint 恢复 + 高亮消除 |
| 5 | 后端返回无 field 的通用错误 | Toast 弹出（降级行为不变） |
| 6 | 编辑任务页 → 同样测试 | 行为一致 |

### 7.2 回归验收

| # | 场景 | 期望 |
| --- | --- | --- |
| R1 | 标签管理 → 创建空名标签 | 模态框内 `.tg-err` 仍正常（不受影响） |
| R2 | Dashboard 退役弹窗 → 不选任务提交 | 模态框内 `.cp-modal-err` 仍正常 |
| R3 | 成功操作（如正常添加任务） | CpToast.success 仍正常弹出 |
| R4 | 网络错误（如断网提交） | CpToast.error('网络错误') 仍正常（HTTP error 降级路径） |

### 7.3 门禁命令

```
# 单元测试
bash scripts/cronpilot.sh test

# 路由冒烟
.venv-py311/bin/python scripts/smoke_routes.py --check

# UI 契约
python scripts/check_ui_contract.py --check

# 颜色审计
python scripts/audit_hardcoded_colors.py --check

# restart + 浏览器验证
bash scripts/cronpilot.sh restart --daemon
curl -s http://127.0.0.1:5001/rbac/login -w "%{http_code}"
```

## 8. 风险

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| 内联文案导致布局偏移 | 低：`.tf-error-msg` 高度约 20px，偏移可接受 | 使用 `min-height` 预留可选；当前表单间距足够 |
| 其他 js-ajax-form 表单（非 task\_form）受影响 | 低：目前只有 task\_form 有 `data-field` 标注；其他表单走 Toast 降级 | 逻辑为「有 field → 找元素 → 找到才插入；找不到 → Toast 降级」 |
| CpToast.success 被误取消 | 中：成功提示消失 | 仅在 `errcode !== 0 && field` 时取消 Toast；success 路径不变 |
| Vue CronFormValidator 冲突 | 中：`#cron-form-validator` 已有 `.cron-field-inline-error`（URL/JSON 客户端校验），服务端返回 `field=cron_div` 时可能出现双重错误信息 | Vue 仅处理 URL/JSON 客户端校验；cron 格式由后端校验。两者字段不重叠（Vue 管 URL/JSON 格式，后端管 cron 表达式非空/格式），不会同时触发 |
| scrollIntoView + fadeIn 时序 | 低：先插入 DOM 再触发 scrollIntoView 时，fadeIn 动画可能在滚动完成前开始，用户短暂看到空位置 | CSS fadeIn 仅 0.15s，肉眼几乎不可察；且 `.tf-error-msg` 在 DOM 中已有文本内容，仅透明度过渡 |

## 9. 全量审计：Redesign 页面错误反馈机制

对 `app/templates/redesign/` 下全部 40 个模板文件进行了错误反馈机制审计，共发现 **62 个错误场景**，分为三类：

### 9.1 审计总览

| 分类 | 数量 | 说明 |
| --- | --- | --- |
| **A · 需要修复** | **15** | 表单字段级校验错误使用 Toast 不合理，应改为字段旁内联文案 |
| B · 合理使用 | 27 | 操作确认/系统错误/网络错误等非表单字段场景，Toast 是正确选择 |
| B+ · 基本合理 | 3 | 认证页 SSR banner，距表单近（≤50px），改造收益低 |
| C · 已有内联 | 20 | 模态框 `.tg-err` / `.cp-modal-err` / `.dm-err` / HTML5 原生校验等 |
| 缺失 | 3 | 无任何错误处理（标签建议 fetch 静默失败 + 批量操作 POST 无 handler） |

### 9.2 A 类详细清单（18 项）

#### ① js-ajax-form 表单服务端校验 → Toast（8 项）

| 页面 | 错误场景 | 当前机制 | 是否有 data-field | 建议 |
| --- | --- | --- | --- | --- |
| `task_form.html` | 服务端返回 field key 的校验错误 | Toast + 高亮 + 滚动 | ✓（9 个字段） | 改为内联文案，不弹 Toast |
| `cron_retire.html` | 退役原因校验 | Toast | ✗ | 补 `data-field="reason"` + 内联 |
| `change_password.html` | 密码错误/不匹配 | Toast | ✗ | 补 data-field + 内联 |
| `user_form.html` | 用户名冲突/角色/组等 | Toast | ✗ | 补 data-field + 内联 |
| `user_profile.html` | 昵称/邮箱等 | Toast | ✗ | 补 data-field + 内联 |
| `group_form.html` | 组名重复等 | Toast | ✗ | 补 data-field + 内联 |
| `users_set_active.html` | 停用原因为空 | Toast | ✗ | 补 data-field + 内联 |

注：7 个表单缺少 `data-field`，需在后端返回 field key + 模板补 `data-field` 后才能启用内联文案。

#### ② SSR 页顶 Banner 的表单（5 项）

| 页面 | 当前机制 | 建议 |
| --- | --- | --- |
| `change_password.html` | `pw-alert-err` 页顶 banner | 改为 AJAX 提交 + 字段内联；或保持 banner 但移至字段旁 |
| `user_form.html` | `uf-alert-err` 页顶 banner | 同上 |
| `user_profile.html` | `pw-alert-err` 页顶 banner | 同上 |
| `group_form.html` | `gf-alert-err` 页顶 banner | 同上 |
| `users_set_active.html` | `usa-alert-err` 页顶 banner | 同上 |

注：这些表单同时是 `js-ajax-form`，AJAX 成功时走 Toast，但 SSR fallback 时走页顶 banner。SSR banner 可在 AJAX 化完善后逐步移除。

#### ③ 认证页 SSR Banner（3 项 → 降级为 B+ 类）

| 页面 | 当前机制 | 分类 | 理由 |
| --- | --- | --- | --- |
| `login.html` | `auth-msg-err` banner | **B+（基本合理）** | Banner 在表单正上方（≤50px），非长表单，已满足接近性原则 |
| `register.html` | `auth-msg-err` banner | **B+** | 同上 |
| `complete_profile.html` | `auth-msg-err` banner | **B+** | 同上 |

注：认证页使用原生 POST（非 AJAX），SSR banner 在表单上方、距离输入区域近，改造收益不明显。从 A 类降级为 B+（经 Review 修正）。

### 9.3 实施优先级建议

| 优先级 | 批次 | 页面 | 改动量 | 理由 |
| --- | --- | --- | --- | --- |
| P0 | Phase 1 | `task_form.html` | 最小：已有 data-field，仅改 JS + CSS | 使用频率最高的表单；已有字段映射基础 |
| P1 | Phase 2 | `user_form / group_form / cron_retire / users_set_active` | 中等：需后端补 field key + 模板补 data-field | 管理操作表单，使用频率中等 |
| P1 | Phase 3 | `change_password / user_profile` | 中等：同上 | 个人设置表单 |
| P2 | Phase 4 | 认证页 banner 优化 | 较大：需要评估是否改为 AJAX | 使用频率低（仅登录/注册时）；当前 banner 基本可用 |

### 9.4 缺失错误处理（待补）

| 页面 | 场景 | 建议 |
| --- | --- | --- |
| `task_form.html` | 标签建议 fetch 失败 | 静默隐藏可接受（非阻断操作），建议 console.warn |
| `registration_review.html` | 批量审批 POST 失败 | 应补 error 回调 + Toast |

## 10. Phase 2 — 全 RBAC 表单覆盖设计

### 10.1 问题

Phase 1 仅覆盖了 `task_form.html`，审计中识别的其余 7 张 A 类表单仍使用全局 Toast 反馈字段级校验错误。用户在修改密码、管理用户/组、下线任务、停用用户、编辑个人资料时，仍面临「错误文案与出错字段空间分离」的体验问题。

### 10.2 方案概览

基于 Phase 1 已验证的内联文案架构，将覆盖范围扩展到全部 RBAC 表单。核心工作分为四层：

#### ① 基础设施提升：CSS 共享化

| 变更 | 原因 | 风险评估 |
| --- | --- | --- |
| `.tf-error-msg` 从 `redesign-pages.css`（`.cp-page-task-form` 作用域下）移入 `redesign-components.css`（全局） | Phase 1 样式限定在 task-form 页面作用域下，其他页面的 `.tf-error-msg` 无样式。按 CSS 归属决策树，跨页面复用的 UI 组件应放在 `redesign-components.css` | **低风险**：选择器特异性降低（从 `.cp-page-task-form .tf-error-msg` 降为 `.tf-error-msg`），但该类仅由 JS 动态创建，无意外匹配可能 |

#### ② 基础设施提升：JS hint 选择器泛化

| 变更 | 原因 | 风险评估 |
| --- | --- | --- |
| hint 选择器从 `.tf-hint` 改为 `[class*="-hint"]`（CSS attribute 选择器通配） | 各 RBAC 页面使用不同前缀：`cr-hint`（cron\_retire）、`pw-hint`（password）、`uf-hint`（user\_form）、`gf-hint`（group\_form）、`usa-hint`（set\_active）。统一查找避免逐页定制 | **低风险**：`[class*="-hint"]` 匹配所有含 `-hint` 的类。审计确认无误匹配（如 `kbd-hint` 在非表单区域，不包含在 `[data-field]` 容器内） |
| `task_form.html` 的 input 事件 hint 恢复从 `classList.contains('tf-hint')` 改为正则 `/\b\w+-hint\b/` | 一致性：与 `common-redesign.js` 的泛化逻辑对齐 | **极低**：仅影响清除时的 hint 恢复识别，正则语义等价于原有判断 |

#### ③ 后端 field key 返回

Phase 1 中 `task_form` 的后端已返回 `data.field`。Phase 2 需要为 RBAC 表单补充：

| 表单 | 后端策略 | field key 映射规则 | 合理性分析 |
| --- | --- | --- | --- |
| `cron_retire` | 在视图层 `views.py` 直接 `data={'field': 'reason'}` | 该表单仅一个输入字段（`reason`），无歧义 | **合理**：最小 diff，不改服务层 |
| `users_set_active` | 视图层根据错误消息关键词（`'缘由'`）匹配 → `field='reason'` | 该表单仅一个输入字段 | **合理**：关键词匹配可靠（服务层只有两个含「缘由」的错误消息） |
| `group_form` | 辅助函数 `_group_field_data(result)`：`'名称'` → `gf-name`，`'描述'` → `gf-desc` | 两个字段，关键词不重叠 | **合理**：映射清晰 |
| `change_password` | 辅助函数 `_password_field_data(msg)`：`'当前密码'` → `pwd-old`，`'新密码'` → `pwd-new`，`'不一致'` → `pwd-confirm` | 三个字段，关键词在服务层消息中唯一对应 | **合理**：覆盖所有 5 种错误消息 |
| `user_form` | `_users_form_response` 自动推断：当 `template` 包含 `'user_form'` 且错误时，调用 `_user_field_data(msg)` | `'用户名'/'已存在'` → `uf-username`，`'岗位'` → `uf-job-title-select`，`'业务组'/'全局权限'` → `uf-group-select`，`'角色'` → `uf-role-select`，`'邮箱'` → `uf-email` | **合理但需注意**：关键词匹配依赖服务层消息文案不变。5 个字段关键词不重叠，覆盖了 `create_user` / `update_user` / `validate_groups_for_role` 的所有错误路径。潜在风险：新增错误消息如不含上述关键词，将降级为 Toast（安全降级） |
| `user_profile` | 辅助函数 `_profile_field_data(msg)`：`'邮箱'` → `pf-email`，`'花名'` → `pf-nickname`，`'岗位'` → `pf-job-title` | 三个字段；服务层可能返回多错误用 `'；'` 连接，取首个错误映射 | **合理**：取首个错误（最先触发）聚焦对应字段，符合「逐个修正」的 UX 模式 |

#### ④ 模板变更

每张表单的模板变更包含以下 3 项标准化改造：

| 改造项 | 原因 | 影响评估 |
| --- | --- | --- |
| 添加 `novalidate` 到 `<form>` | Phase 1 已验证：HTML5 `required` / `min` / `max` 触发浏览器原生校验气泡，样式与自定义内联文案冲突且不可控 | **无功能影响**：所有校验由后端服务层完成，前端不再双重校验 |
| 移除 `required` / `minlength` 属性 | 配合 `novalidate`；即使有 `novalidate`，部分浏览器在 JS 调用 `checkValidity()` 时仍检查这些属性 | **无功能影响**：后端已有等价校验 |
| 添加 `data-field="xxx"` 到输入字段所在的容器 `div` | JS 通过 `[data-field]` 选择器定位错误显示位置 | **无视觉影响**：data 属性不影响渲染；与 Phase 1 的 `task_form` 模式完全一致 |

### 10.3 逐表单变更明细与合理性评估

#### 10.3.1 cron\_retire.html — 下线任务

| 项目 | 内容 |
| --- | --- |
| field 数量 | 1（`reason` — 下线原因） |
| 后端 field key | `views.py` 在 `retire_cron_by_id()` 返回错误时添加 `data={'field': 'reason'}` |
| 错误消息 | 「请填写下线原因」「下线原因不能超过500字」 |
| 模板变更 | ① `novalidate`；② `data-field="reason"`；③ 移除 `required` |
| 合理性 | **完全合理**。该表单仅有一个输入字段，field key 映射无歧义。改造量最小，风险最低 |

#### 10.3.2 users\_set\_active.html — 停用用户

| 项目 | 内容 |
| --- | --- |
| field 数量 | 1（`reason` — 停用缘由） |
| 后端 field key | 视图层检查 `'缘由' in result['msg']` → `field='reason'` |
| 错误消息 | 「请填写缘由」「缘由最长 500 字」 |
| 模板变更 | 同 cron\_retire：① `novalidate`；② `data-field="reason"`；③ 移除 `required` |
| 合理性 | **完全合理**。与 cron\_retire 同构 |

#### 10.3.3 group\_form.html — 新建/编辑业务组

| 项目 | 内容 |
| --- | --- |
| field 数量 | 2（`gf-name` — 组名，`gf-desc` — 说明） |
| 后端 field key | `_group_field_data(result)`：`'名称'` → `gf-name`，`'描述'` → `gf-desc` |
| 错误消息 | 「名称不能为空」「名称最长 64 字符」「名称已存在」「描述最长 255 字符」 |
| 模板变更 | ① `novalidate`；② 两个字段容器加 `data-field`；③ 移除 `required` |
| 自动推断 | `_users_form_response` 自动检测 `template='redesign/group_form.html'` 时调用 `_group_field_data`，无需在每个调用点显式传 data |
| 合理性 | **合理**。组名和描述的错误消息关键词不重叠，映射可靠 |

#### 10.3.4 change\_password.html — 修改密码

| 项目 | 内容 |
| --- | --- |
| field 数量 | 3（`pwd-old`、`pwd-new`、`pwd-confirm`） |
| 后端 field key | `_password_field_data(msg)`：`'当前密码'` → `pwd-old`，`'新密码'` → `pwd-new`，`'不一致'` → `pwd-confirm` |
| 错误消息 | 共 5 条，全部有明确关键词对应：请填写当前密码 / 当前密码不正确 / 请填写新密码 / 新密码至少 6 位 / 两次输入的新密码不一致 / 新密码不能与当前密码相同 |
| 模板变更 | ① `novalidate`；② 3 行加 `data-field`；③ 移除 `required` 和 `minlength="6"` |
| 特殊考量 | 该页面有密码可见性切换按钮（`.pw-toggle`）和退出登录按钮，本轮不涉及；JS 中 `required` 动态设置无需清理（无动态 required） |
| 合理性 | **合理**。密码表单是内联错误收益最高的场景之一（用户连续输错密码时需立即知道哪个字段有问题） |

#### 10.3.5 user\_form.html — 新建/编辑用户

| 项目 | 内容 |
| --- | --- |
| field 数量 | 5（`uf-username`、`uf-role-select`、`uf-email`、`uf-job-title-select`、`uf-group-select`） |
| 后端 field key | `_users_form_response` 自动推断 + `_user_field_data(msg)` |
| 模板变更 | ① `novalidate`；② 5 个字段容器加 `data-field`；③ 移除 `required`；④ 移除 JS 中动态设置 `jtOther.required` 的代码 |
| 特殊考量 | - **多错误源**：`create_user`、`update_user`、`validate_groups_for_role`、`set_user_groups` 四个函数均可返回错误，通过统一的 `_users_form_response` + 自动推断覆盖 - **edit 模式无 username**：编辑时用户名字段为 disabled，后端不校验，不会触发 username 错误 - **全局权限选项**：`'仅 admin 角色可选择「全部」'` 中包含关键词 `'全部'`，映射到 `uf-group-select` |
| 合理性 | **合理但复杂度最高**。关键词匹配方案的 edge case 在于新增错误消息如不含预期关键词会降级为 Toast（安全降级，不会丢失信息）。长期可考虑让服务层直接返回 field key |

#### 10.3.6 user\_profile.html — 个人资料

| 项目 | 内容 |
| --- | --- |
| field 数量 | 3（`pf-nickname`、`pf-email`、`pf-job-title`） |
| 后端 field key | `_profile_field_data(msg)`：取首个错误（`'；'` 分隔）的关键词匹配 |
| 模板变更 | ① `novalidate`；② 3 个字段加 `data-field`；③ 移除 `required`；④ 移除 JS 中 `prop('required', true/false)` |
| 特殊考量 | 该页面有 view/edit 双模式切换，`data-field` 仅加在 edit 模式的表单字段上（view 模式无提交行为） |
| 合理性 | **合理**。服务层 `update_own_profile` 可能返回多个错误（`'；'` 连接），取首个聚焦符合逐个修正的 UX 模式 |

### 10.4 变更合理性总评

#### 合理的方面

1. **架构一致性**：Phase 2 完全复用 Phase 1 已验证的机制（`.tf-error-msg` + `data-field` + `novalidate`），无新增架构概念
2. **CSS 归属正确**：按决策树，跨页面复用的组件样式归 `redesign-components.css`
3. **安全降级**：所有 field key 匹配失败均降级为 Toast，不丢失任何错误信息
4. **后向兼容**：`_users_form_response` 的 `data` 参数默认 `None`，不影响已有调用
5. **无 DOM 新增**：`.tf-error-msg` 由 JS 动态管理，模板仅添加 `data-field` 属性和 `novalidate`

#### 需要关注的风险点

| 风险 | 概率 | 影响 | 缓解 |
| --- | --- | --- | --- |
| 关键词匹配方案的脆弱性 | 低 | 新增错误消息可能未被匹配到 field | 降级为 Toast（安全降级）。长期可在服务层直接返回 `field` key |
| `[class*="-hint"]` 误匹配 | 极低 | 非表单内的 hint 元素被误操作 | 选择器作用域限定在 `$el.find()` 内，`$el` 是 `[data-field]` 容器，非表单区域不受影响 |
| 移除 `required` 后无前端即时反馈 | 中 | 用户提交空表单后需等待服务端响应才看到错误 | AJAX 响应通常 < 200ms，体感差异极小。且后端校验是唯一可信来源，消除了前后端校验不一致的可能 |
| SSR fallback 路径 | 低 | JS 禁用或 AJAX 失败时，表单走 SSR POST，错误以页顶 banner 显示 | banner 机制保留不变，不影响 fallback |

### 10.5 准确性验证

以下命令已执行并通过：

```
# 全量测试 634 pass / 6 skip / 0 fail
.venv-py311/bin/python -m unittest discover tests -v

# API 响应验证：change_password 空提交
curl -X POST /rbac/password -H "X-Requested-With: XMLHttpRequest" -d "old_password=&new_password=&confirm_password="
→ {"data":{"field":"pwd-old"},"errcode":1,"errmsg":"请填写当前密码"}

# API 响应验证：change_password 密码过短
curl -X POST /rbac/password -d "old_password=xxx&new_password=abc&confirm_password=abc"
→ {"data":{"field":"pwd-new"},"errcode":1,"errmsg":"新密码至少 6 位"}

# 静态门禁
python scripts/check_ui_contract.py --check  → 0 violations
python scripts/audit_hardcoded_colors.py --check  → 未发现硬编码颜色
sqlite3 datas/job_log.sqlite ".tables"  → 14 张表完整
```

## 版本历史

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| rev.1 | 2026-09-01 | 初版：问题分析 + 业界对比 + 方案 A 设计 + Demo |
| rev.2 | 2026-09-01 | 新增：全量审计 62 个错误场景 + 实施优先级 + 缺失处理 |
| rev.3 | 2026-09-01 | Review 修正：认证页 3 项 A→B+ 降级；补充 hint 恢复策略、Section 插入位置、ARIA role="alert"、Vue 冲突风险、动画时序风险 |
| rev.4 | 2026-09-01 | Phase 2 全 RBAC 表单覆盖设计：6 张表单逐表变更明细、合理性评估、风险分析、准确性验证 |

[文档索引](index.html) · [Markdown](表单字段级错误提示设计.md) · [索引](index.html)

---

[← 文档索引（HTML）](../index.html) · [← 文档索引（Markdown）](../index.md)
