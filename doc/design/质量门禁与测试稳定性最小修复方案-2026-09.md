# 质量门禁与测试稳定性最小修复方案（2026-09）

> HTML 版：[质量门禁与测试稳定性最小修复方案-2026-09.html](质量门禁与测试稳定性最小修复方案-2026-09.html) · [文档索引](../index.html) · [索引 Markdown](../index.md)

# 质量门禁与测试稳定性最小修复方案（2026-09-04）

状态：已确认（本轮） · 范围：仅修复验证链路阻断项，不做业务能力变更

## 1. 问题

- `tests.test_rbac_scope.TestScopeIntegration` 单独运行报 `rbac_registration_requests` 表缺失。
- `scripts/verify_golden_path.sh` 的 `docs_rfc` 检查固定 URL 返回 404。
- `scripts/check_css_token_reachability.py --check` 对注释里的 `animation:` 文本误报为缺失 keyframe。
- `scripts/check_postmortem_completeness.py --check` 报告 `doc/postmortem/index.html` 缺少对应 MD。

## 2. 根因

| 问题 | 根因 |
| --- | --- |
| Scope 集成测试单跑失败 | 测试初始化对登录链路依赖的模型表导入不完整，存在隐式顺序依赖。 |
| golden path 文档检查 404 | 脚本内 URL 契约未跟随文档目录结构演进（`deps/` 子目录）。 |
| CSS reachability 误报 | 静态解析未先剥离注释，注释中的示例文本被当成真实 CSS 声明。 |
| postmortem completeness 失败 | postmortem 索引页存在 HTML/MD 对照遗漏，未执行一次完整文档同步闭环。 |

## 3. 方案

采用最小改动策略，逐项修复验证阻断点：

1. 补齐测试模型导入，消除单测顺序依赖。
2. 更新 `smoke_http.sh` 的 RFC 检查 URL 为真实路径。
3. 在 reachability 脚本中先去除 CSS 注释，再解析动画引用。
4. 执行并提交文档同步产物，补齐 `doc/postmortem/index.md`。

## 4. 范围

| 类型 | 路径 | 说明 |
| --- | --- | --- |
| 测试 | `tests/test_rbac_scope.py` | 仅补导入，确保建表完整。 |
| 脚本 | `scripts/smoke_http.sh` | 仅修正 docs\_rfc URL。 |
| 脚本 | `scripts/check_css_token_reachability.py` | 仅修复注释误判。 |
| 文档同步 | `doc/postmortem/index.md` | 由同步脚本生成。 |

**不做：**不改 RBAC 权限策略、不改 API 行为、不改任何页面交互与样式设计。

## 5. 分批实施

| 批次 | 动作 | 独立验收 |
| --- | --- | --- |
| Batch 1 | 修复 `test_rbac_scope` 单跑失败（补齐模型导入）。 | `.venv-py311/bin/python -m unittest tests.test_rbac_scope.TestScopeIntegration -v` |
| Batch 2 | 修复 `docs_rfc` 检查路径。 | `bash scripts/verify_golden_path.sh` |
| Batch 3 | 修复 CSS reachability 注释误报。 | `.venv-py311/bin/python scripts/check_css_token_reachability.py --check` |
| Batch 4 | 补齐 postmortem 索引 Markdown。 | `.venv-py311/bin/python scripts/check_postmortem_completeness.py --check` |

## 6. 验收命令（最终全量）

```
.venv-py311/bin/ruff check app/
.venv-py311/bin/python scripts/check_ui_contract.py --check
.venv-py311/bin/python scripts/audit_hardcoded_colors.py --check
.venv-py311/bin/python scripts/check_css_token_reachability.py --check
.venv-py311/bin/python scripts/check_doc_completeness.py --check
.venv-py311/bin/python scripts/check_doc_links.py --check
.venv-py311/bin/python scripts/check_version_consistency.py --check
.venv-py311/bin/python scripts/check_opt_consistency.py --check
.venv-py311/bin/python scripts/check_postmortem_completeness.py --check
.venv-py311/bin/python scripts/smoke_routes.py --check
bash scripts/cronpilot.sh test
bash scripts/verify_golden_path.sh
```

## 7. 风险与回滚

- 风险低：仅测试/脚本/文档同步层改动，不触达业务逻辑。
- 若出现副作用，按文件级回滚（单批回滚不影响其他批次）。
- 对运行时行为的唯一潜在影响点是 `verify_golden_path.sh` 的验收输出，不影响线上服务逻辑。

[文档索引](../index.html) · [Markdown 版](质量门禁与测试稳定性最小修复方案-2026-09.md)

---

[← 文档索引（HTML）](../index.html) · [← 文档索引（Markdown）](../index.md)
