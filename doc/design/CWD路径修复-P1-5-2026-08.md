# CronPilot CWD 路径修复 P1-5 (2026-08)

> HTML 版：[CWD路径修复-P1-5-2026-08.html](CWD路径修复-P1-5-2026-08.html) · [文档索引](../index.html) · [索引 Markdown](../index.md)

# CronPilot CWD 路径修复 P1-5 (2026-08)

|  |  |
| --- | --- |
| 创建日期 | 2026-08-28 |
| 编号 | P1-5（代码质量优化方案） |
| 来源 | 2026-08-28 全面 Code Review |
| 状态 | 已实施（2026-08-28） |
| 关联文档 | [代码质量优化方案（总览）](代码质量优化方案-2026-08.html) |

---

## 1. 问题

`configs.py` 第 22 行 `cp.read('conf.ini', encoding='utf-8')` 使用 CWD 相对路径读取配置文件。`ConfigParser.read()` 在文件不存在时**静默返回空列表**（不报错），导致所有配置项变为空字符串或默认值。如果进程的工作目录不是项目根目录，配置将完全失效。

## 2. 根因

上游 `xiaoniu_cron` 原始代码使用相对路径。启动脚本（`start_local_full.sh`、`run_production.sh`）都会 `cd` 到项目根目录后再启动 Python，因此问题从未在正常部署中暴露。

同文件第 7 行已定义 `_proj_root = os.path.dirname(os.path.abspath(__file__))` 并用于 SQLite URL 解析，但 `conf.ini` 读取未使用此变量——属于"修了一半"的历史遗留。

## 3. 方案

```
# configs.py 第 22 行
# 修复前
cp.read('conf.ini', encoding='utf-8')

# 修复后
cp.read(os.path.join(_proj_root, 'conf.ini'), encoding='utf-8')
```

## 4. 范围

| 文件 | 改动 |
| --- | --- |
| `configs.py` | 第 22 行，`'conf.ini'` → `os.path.join(_proj_root, 'conf.ini')` |

**不做的事**：不改变 `configs()` 的接口或返回结构；不修改其他文件中对 `configs()` 的调用方式。

## 5. 分批

单行修改，**单步交付**。

## 6. 验收

```
# 单元测试
bash scripts/cronpilot.sh test
# 期望：634+ tests, OK

# 路由冒烟（验证配置读取链路正常）
python scripts/smoke_routes.py --check
# 期望：86/86 routes pass

# ruff
ruff check configs.py
# 期望：0 violations
```

## 7. 风险

| 风险 | 等级 | 说明 |
| --- | --- | --- |
| 回归风险 | 极低 | `_proj_root` 就是 `configs.py` 所在目录（= 项目根），与现有启动脚本 `cd` 到项目根后的行为完全等价 |
| 部署兼容 | 无 | Docker 和裸机部署均将项目完整复制，`conf.ini` 始终与 `configs.py` 同目录 |

---

本文档源自 2026-08-28 全面 Code Review · P1-5 CWD 路径修复。  
[代码质量优化总览](代码质量优化方案-2026-08.html) · [Markdown 版](CWD路径修复-P1-5-2026-08.md)

---

[← 文档索引（HTML）](../index.html) · [← 文档索引（Markdown）](../index.md)
