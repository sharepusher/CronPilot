# 已合并补丁说明 · 删除记录

本文记录从 `doc/_pending_sync/` **删除**的 `*-同步.md` 补丁说明书。  
这些文件**不是**正式文档，仅用于 root 属主无法直接改主文件时的合并指引。

**删除提交：** `7b5fa94`（2026-06-22）· 分支 `main`  
**删除方式：** 工作区移除（上述三文件从未纳入 Git 跟踪）；删除原因与对照证据记入本文件并随 `doc/文档同步说明.md` 一并提交。

---

## 1. `README-依赖升级RFC同步.md`

| 项 | 内容 |
|----|------|
| **原用途** | 说明在 `README.md` 中更新 Python 行与依赖升级 RFC 链接 |
| **删除原因** | 待改内容已全部出现在主文档，无需保留操作说明 |

**对照证据：**

| 待办 | 主文档位置 |
|------|------------|
| Python 行：`gevent 20 栈见 doc/依赖升级RFC.html Tier 2` | [README.md](../../README.md) 非 Docker 部署表 · Python 行 |
| 可选：文档节 RFC 链接 | [README.md](../../README.md) 第 17 行（Tier 0→1→2 路径）；文档列表含 [依赖升级 RFC](../依赖升级RFC.html) |

---

## 2. `RELEASE_NOTES-Tier0同步.md`

| 项 | 内容 |
|----|------|
| **原用途** | 指引执行 `sudo bash scripts/apply_tier0_release_notes.sh` 写入 Tier 0 Unreleased |
| **删除原因** | Tier 0 / Tier 1 条目已直接合入 Release Notes，不再依赖该脚本路径 |

**对照证据：**

| 待办 | 主文档位置 |
|------|------------|
| `[Unreleased]` · Tier 0（`flask db`、退役 Flask-Script 等） | [RELEASE_NOTES.md](../../RELEASE_NOTES.md)、[doc/RELEASE_NOTES.html](../RELEASE_NOTES.html)、[doc/RELEASE_NOTES.md](../RELEASE_NOTES.md) |

---

## 3. `非Docker部署指南-依赖升级RFC同步.md`

| 项 | 内容 |
|----|------|
| **原用途** | 在 `doc/非Docker部署指南.html` 补 §2 Python、§9 FAQ、§10 相关文档 RFC 链接 |
| **删除原因** | 与 [依赖升级 RFC v1.1](../依赖升级RFC.html) 对齐的段落已在主 HTML 中；§9 迁移 FAQ 已按 Tier 0 交付更新为 `flask db` 表述 |

**对照证据：**

| 待办 | 主文档位置 |
|------|------------|
| §2 环境要求 · Python + RFC Tier 2 | [非Docker部署指南.html](../非Docker部署指南.html) §2 表格 |
| §9 `pip install gevent` 失败 | 同上 §9 常见问题 |
| §9 迁移 CLI（原稿写 `manage.py db`） | 同上 §9「数据库迁移 CLI」+ `flask db`（优于原待办文案） |
| §10 相关文档首项 RFC | 同上 §10 列表 |

合入后校验：`python scripts/html_docs_to_markdown.py --check` 已通过。

---

## 后续约定

- 新的 `*-同步.md` 补丁说明：**合入主文档并验收后**，在本文件追加一条删除记录，再移除该说明文件。
- `doc/_pending_sync/` 临时全文副本：仅由 `sync_all_docs.py` 写入 + `pending_apply.manifest` 登记；**禁止**将根目录主文档手工复制到此处入库。
- 目录索引见 [README.md](README.md)（非项目 README）。

## 历史问题：陈旧 `README.md` 副本（已修复 2026-06-22）

| 项 | 说明 |
|----|------|
| **现象** | 曾跟踪的 `doc/_pending_sync/README.md`（168 行）比根 `README.md`（316 行）旧 |
| **原因** | 见 [文档同步说明.md](../文档同步说明.md) §为何会出现「pending 里 README 比根目录旧」 |
| **处置** | 替换为目录说明；`apply_pending_docs.sh` 改为 manifest + 新旧校验 |
