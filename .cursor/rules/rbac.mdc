---
description: CronPilot RBAC v4 + Resource Scope（OPT-P2-10 / OPT-P2-12）
globs: app/rbac/**/*.py,datas/model/rbac_*.py,datas/model/resource_group.py,datas/model/user_group.py,app/main/views.py,configs.py,conf.ini.example,app/__init__.py,tests/test_rbac*.py
alwaysApply: false
---

# RBAC + Resource Scope 开发规范

权威详设：`doc/RBAC架构设计方案.html`（**v4**）· Scope：`doc/资源隔离与Scope设计.html`（**OPT-P2-12**）· 落地：`doc/RBAC落地路线.html`、`doc/资源隔离落地路线.html`。

## 范围声明

- OPT-P2-10（三角色 Capability）与 OPT-P2-12（Resource Scope Visibility）均须用户明确要求后演进；**勿**未确认越级实现 S6（API Scope / Tenant）。
- 遵守 `cronpilot-project.mdc` 交付闭环与最小 diff。

## 分层约定

| 职责 | 位置 | 说明 |
|------|------|------|
| 权限判断 | `app/rbac/policy.py` | `ROLE_PERMISSIONS`、`has_permission`、`SCOPE_BYPASS_ROLES`（admin）、`check_policy` stub |
| Scope | `app/rbac/scope.py` | `build_scope_filter_clause`、`has_scope`、`normalize_scope_fields` |
| 统一授权 | `app/rbac/authorize.py` | `authorize`：Permission → Scope → Policy |
| 装饰器 | `app/rbac/decorators.py` | `require_permission`；`require_login`（自助改密）；`authorize_resource`（对象级） |
| 模板上下文 | `app/rbac/context.py` | `make_has_perm`；`current_group_ids` |
| 用户/组/审计 | `app/rbac/services.py` | 用户 CRUD、`change_own_password`、业务组 CRUD、`set_user_groups`、审计 |
| 数据模型 | `rbac_*`、`resource_group`、`user_group`；`cron_infos.scope_type/group_id` | |

## 允许改动白名单（Scope 相关追加）

| 文件 | 允许改动 |
|------|----------|
| `app/rbac/**` | 含 `scope.py`、`authorize.py`、groups 路由 |
| `datas/model/resource_group.py`、`user_group.py`、`cron_infos.py`（Scope 列） | 模型 |
| `app/main/views.py` | `@require_permission` + load 后 **`authorize_resource` / `build_scope_filter_clause`**；禁止散落 `if group_id` |
| `app/services/cron_service.py` | **仅**透传 `scope_type`/`group_id` 落库，**禁止**角色/组判断 |
| `app/templates/cron_*.html`、`rbac/groups*.html`、`rbac/users_*.html`、`rbac/_nav.html` | Scope UI |
| `tests/test_rbac_scope.py` | Scope 单测 |
| `scripts/ensure_sqlite_tables.py` | 建表/补列 |
| 文档 | `doc/资源隔离*.html`、`RELEASE_NOTES`、`交付状态与路线图` |

**不在白名单、默认不改**：`app/api/views.py`（`access_token` 独立，Scope 首期除外）。

## 三角色与权限（勿增第四内置角色）

```python
ROLE_PERMISSIONS = {
    'viewer':   {'cron:read', 'log:read'},
    'operator': {'cron:read', 'cron:write', 'log:read', 'operation:read'},
    'admin':    {'cron:read', 'cron:write', 'cron:retire',
                 'log:read', 'operation:read', 'user:manage', 'audit:read'},
}
SCOPE_BYPASS_ROLES = frozenset({'admin'})
```

业务组管理复用 **`user:manage`**（不新增 `group:manage`）。Role **不得**绑定 Group。

## Session

- 登录写 `role`、`user_id`、`group_ids`
- 用户组变更后须**重新登录**生效

## 禁止事项

- ❌ `before_request` / `route_registry` / JWT / 多表角色权限引擎
- ❌ 角色爆炸（研发管理员等）或 Role↔Group
- ❌ 在 views/service 手写 `if cron.group_id != …`（必须 `authorize` / `build_scope_filter_clause`）
- ❌ 修改 `app/api/views.py` 的 `access_token` 语义（除非单独 RFC）
- ❌ 删除业务组（防悬挂）；删除任务（生命周期）
- ❌ `rbac_enable` 旁路
- ❌ 引入 pytest 或新 pip 依赖（除非用户确认）

## 验收

```bash
bash scripts/cronpilot.sh test   # 含 tests.test_rbac_scope
```
