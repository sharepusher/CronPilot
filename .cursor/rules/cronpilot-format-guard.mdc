---
description: 防止 AI 工具格式化清理污染既有代码风格
globs: app/templates/**/*.html,app/**/*.py
alwaysApply: true
---

# 格式风格保留规则

- 修改任意文件时，只允许改动任务明确要求的那部分内容，不允许对任务范围外的代码做任何格式化清理，包括但不限于：引号风格（单引号/双引号）、缩进字符（空格/Tab）、空行数量、import 语句顺序、行尾空格
- 如果发现既有代码风格不一致或有改进空间，应该单独提出来，不要在执行其他任务时顺手「清理」
- 改动前后对比 diff，如果 diff 里出现了与本次任务描述无关的格式变化行，应该自我修正，去掉这些无关变化

## HTML 可见结构与文案约束（追加 2026-07）

**凡改动 HTML 模板时，以下变更须在设计确认时「逐项列出」，不得在实现时顺手完成：**

| 变更类型 | 要求 |
|---|---|
| 表头文案（`<th>` 文本）变更 | 须在设计"范围"章节列出旧值 → 新值 |
| 列数 / colspan 变化 | 须在"交互契约"行写明列结构变化与原因 |
| 可见按钮/链接文案变更 | 须在设计稿明确标注 |
| CSS class 的增删（影响视觉样式） | 须列出受影响元素与类名 |

**自检要求**：交付前执行 `git diff -- app/templates/` 审查 diff，凡出现上表变更但设计稿中未列出者，须先向用户确认再提交。

## 颜色约束（追加 2026-07）

- **禁止**在模板和 Vue 组件中新增硬编码十六进制颜色（`#xxxxxx`），必须使用 `var(--cp-*)` 引用 `app/static/css/console-theme.css` 中的变量。
- 交付前执行 `python scripts/audit_hardcoded_colors.py --check`，存在硬编码颜色则不得提交。
- 需要新颜色时，先在 `console-theme.css` 的 `:root` 中添加语义变量定义，再引用。
