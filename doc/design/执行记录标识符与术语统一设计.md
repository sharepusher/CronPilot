# 执行记录标识符与术语统一设计

> HTML 版：[执行记录标识符与术语统一设计.html](执行记录标识符与术语统一设计.html) · [文档索引](../index.html) · [索引 Markdown](../index.md)

# 执行记录标识符与术语统一设计

**状态**：待确认  |  **作者**：AI Agent  |  **日期**：2026-08-26

## 1. 问题

### 1.1 标识符命名歧义

当前执行记录系统中存在两个标识符，使用了容易混淆的命名：

| 现状 | 数据源 | 实际含义 | 用户认知 |
| --- | --- | --- | --- |
| 列表页列头 **"LOG ID"** → 显示 `18463` | `job_log.id`（数据库自增主键） | 数据库内部序号 | "这是日志的 ID" |
| 详情页标题 **"执行 #ca04c15c"** | `job_log.log_id` UUID 前 8 位 | 跨系统追踪码 | "这是什么？跟列表里的 18463 什么关系？" |
| 详情页元数据 **"Log ID"** → 显示完整 UUID | `job_log.log_id` | 同上 | "又一个 Log ID？跟上面哪个一样？" |

**核心问题**：用户在列表看到 `18463`，点进详情后看到 `#ca04c15c`，两者之间没有任何可见关联。

### 1.2 代码层命名不一致

`app/crons.py` 中同一个值在 4 行内用了两个名字：

```
cronpilot_log_id = str(uuid.uuid1())    # 生成时叫 "log_id"
_ctx_trace_id.set(cronpilot_log_id)      # 设上下文时叫 "trace_id"
```

### 1.3 术语不准确

多处文档和代码注释使用"**回调**"（callback）描述 CronPilot 发起的定时请求。实际上 CronPilot 是主动发起方（trigger），不存在被动回调的语义。准确术语应为"**定时触发**"或"**调度请求**"。

"**回调**"出现在约 60+ 文件中，共 200+ 处引用。

## 2. 根因

1. **早期设计未区分标识符用途**：`log_id` 字段名暗示"日志的 ID"，但实际用途是跨系统追踪码（trace ID），与数据库主键 `id` 语义冲突。
2. **UI 列头 "LOG ID" 使用了数据库主键**：列表页 "LOG ID" 展示 `id`（自增序号），详情页 "执行 #" 展示 `log_id`（UUID），从列表到详情的导航链路中标识符发生了无声切换。
3. **外部协议参数命名传播**：`cronpilot_log_id` 作为定时触发请求的参数名传递给目标服务，名称中的 "log" 进一步强化了歧义。
4. **术语惯性**：项目早期（xiaoniu\_cron 时期）使用"回调"描述定时触发行为，后续文档沿用未更正。

## 3. 方案

### 3.1 标识符重命名（一步到位）

CronPilot 是开源项目，当前无已知外部集成方，可直接重命名而无需兼容过渡。

| 变更项 | 旧名 | 新名 | 影响标记 |
| --- | --- | --- | --- |
| Model 属性 | `JobLog.log_id` | `JobLog.trace_id` | 内部 |
| Model 属性 | `JobLogItems.log_id` | `JobLogItems.trace_id` | 内部 |
| Model 属性 | `JobHealth.last_run_log_id` | `JobHealth.last_run_trace_id` | 内部 |
| 数据库列 | `job_log.log_id` `job_log_items.log_id` `job_health.last_run_log_id` | `job_log.trace_id` `job_log_items.trace_id` `job_health.last_run_trace_id` | 迁移 |
| 调度触发参数 | `cronpilot_log_id` | `cronpilot_trace_id` | API |
| API 入参 | `POST /api/cron/add_log` 的 `cronpilot_log_id` | 改为 `cronpilot_trace_id` | API |
| 签名函数 | `get_cronpilot_sign()` — 参数名参与签名 | 参数名跟着改，签名值随之更新 | 协议 |
| 内部变量 | `cronpilot_log_id = str(uuid.uuid1())` | `trace_id = str(uuid.uuid1())` | 内部 |

### 3.2 UI 标识符展示优化

| 页面 | 元素 | 旧展示 | 新展示 |
| --- | --- | --- | --- |
| 执行记录列表 | 列头 | `LOG ID`（显示 `id`） | `#`（显示 `id`，行业惯例序号列标记） |
| 执行记录详情 | 页面标题 | `执行 #ca04c15c`（UUID 前 8 位） | `执行记录 #18463`（数字序号，与列表一致） |
| 执行记录详情 | 面包屑 | `执行 #ca04c15c` | `#18463` |
| 执行记录详情 | 元数据区 | `Log ID: a13114a8-...` | `追踪码 (Trace ID): a13114a8-...`（可复制） |
| 执行记录详情 | 浏览器标签 | `执行 #ca04c15c — CronPilot` | `执行记录 #18463 — CronPilot` |
| v1 列表页 | 列头 | `log_id`（显示 UUID） | `追踪码`（显示 UUID，与 redesign 元数据标签一致） |

**认知连贯性**：用户在列表看到 `#18463` → 点击 → 详情标题 `执行记录 #18463`。追踪码（UUID）作为开发者级信息，降至元数据区域单独标注。

**⚠️ 实现安全边界**：`job_log.id`（自增 PK，int 类型）**绝对不改名**。本次重命名仅涉及 `log_id`（UUID 追踪码，str 类型）→ `trace_id`。改名后 `jl.id` 仍是数字序号（18463），`jl.trace_id` 是 UUID 字符串（a13114a8-...），类型和名称完全不同，混淆风险为零。

### 3.3 术语修正

将用户可见的"回调"术语修正为"定时触发"。

| 位置 | 旧文案 | 新文案 |
| --- | --- | --- |
| 任务表单提示 | `GET 时系统附加 query 参数 cronpilot_log_id / cronpilot_sign` | `定时触发时系统附加 query 参数 cronpilot_trace_id / cronpilot_sign` |
| 任务表单提示 | `系统仍会注入 cronpilot_log_id / cronpilot_sign` | `系统仍会注入 cronpilot_trace_id / cronpilot_sign` |
| API Schema 描述 | `CronPilot 发给业务方的执行记录 ID` | `CronPilot 定时触发时生成的追踪码 (UUID)` |

**关于"回调"全量替换**：项目中约 60+ 文件含"回调"一词（200+ 处），大多在架构文档和设计文档中。本次设计**仅修正用户可见的模板文案和 API Schema 描述**中的术语，文档层面的全量替换作为独立低优先级任务跟踪，避免 diff 膨胀。

## 4. 范围

### 4.1 代码变更清单

| 文件 | 变更类型 | 详情 |
| --- | --- | --- |
| **Model 层（3 文件）** | | |
| `datas/model/job_log.py` | 属性重命名 | `log_id` → `trace_id` |
| `datas/model/job_log_items.py` | 属性重命名 | `log_id` → `trace_id` |
| `datas/model/job_health.py` | 属性重命名 | `last_run_log_id` → `last_run_trace_id` |
| **服务 / 调度层（4 文件）** | | |
| `app/crons.py` | 变量 + 参数名 | `cronpilot_log_id` → `trace_id`（内部）、`cronpilot_trace_id`（外部参数） |
| `app/services/job_health_service.py` | 参数名 | `log_id` → `trace_id` |
| `app/services/job_log_display.py` | 文案修正 | 第 31 行 `'未执行回调'` → `'未执行触发'`（用户可见返回值） |
| `app/common/functions.py` | 无变更 | `get_cronpilot_sign()` 函数本身不引用参数名 |
| **API 层（2 文件）** | | |
| `app/api/views.py` | 参数名 | `cronpilot_log_id` → `cronpilot_trace_id` |
| `app/api/schemas.py` | 字段名 + 描述 | `cronpilot_log_id` → `cronpilot_trace_id` |
| **Repository 层（1 文件）** | | |
| `app/repositories/job_log_repository.py` | 方法名 + 属性引用 | `get_by_log_id()` → `get_by_trace_id()`；`items_for_log_id()` → `items_for_trace_id()` |
| **View 层（1 文件）** | | |
| `app/main/views.py` | 模板变量 + 方法调用 + URL 参数 | `log_id_short` → 改为传递 `jl.id`（数字 ID）；方法调用跟着 Repo 改；`job_log_item_list` 路由的 `request.args.get('log_id')` → `request.args.get('trace_id')` |
| **模板层（8 文件）** | | |
| `app/templates/redesign/execution_logs.html` | 列头 + 注释 | `LOG ID` → `#` |
| `app/templates/redesign/run_inspector.html` | 标题 + 面包屑 + 元数据 | 使用数字 ID；UUID 标注为"追踪码" |
| `app/templates/redesign/_exec_logs_rows.html` | 注释 + CSS class | 注释 `COL 1: LOG ID` → `COL 1: #`；CSS class `log-id` → `record-id`（注意：`data-id="{{ log.id }}"` 展示的是 PK，**不改**） |
| `app/templates/job_log_all_list.html` | 列头 + 数据 | `log_id` → `追踪码` |
| `app/templates/job_log_list.html` | 列头 + 数据 | `log_id` → `追踪码` |
| `app/templates/cron_add.html` | 提示文案 | 参数名 + 术语更新 |
| `app/templates/cron_edit.html` | 提示文案 | 参数名 + 术语更新 |
| `app/templates/redesign/task_form.html` | 提示文案 | 参数名 + 术语更新 |
| **数据库迁移（1 文件）** | | |
| `scripts/ensure_business_tables.py` | 列重命名 | 3 张表的列名迁移（`ALTER TABLE RENAME COLUMN`） |
| **测试（5 文件）** | | |
| `tests/test_cronpilot_sign.py` | 参数名 | 测试数据中 `cronpilot_log_id` → `cronpilot_trace_id` |
| `tests/test_job_log_id.py` | 引用更新 | Model 属性名跟着改 |
| `tests/test_job_health.py` | 引用更新 | Model 属性名跟着改 |
| `tests/test_api_scope_s6.py` | 参数名 | API 调用中的参数名更新 |
| `tests/test_rbac_phase.py` | 引用更新 | 若有 `log_id` 引用则更新 |
| **配置 / 规则（3 文件）** | | |
| `.cursor/rules/cronpilot-project.mdc` | 协议描述 | `cronpilot_log_id` → `cronpilot_trace_id` |
| `AGENTS.md` | 协议描述 | 同上 |
| `app/logging_config.py` | 注释 | 注释中的术语更新 |
| **文档（选择性更新，仅核心文件）** | | |
| `README.md` | 协议表格 | 参数名 + 术语更新 |
| `RELEASE_NOTES.md` | 新增变更条目 | 记录 API Breaking Change |

### 4.2 明确不做

- **文档全量"回调"→"定时触发"替换**：涉及 60+ 文件 200+ 处，作为独立 OPT 跟踪
- **数据库表名变更**：`job_log`、`job_log_items` 表名保持不变
- **函数名变更**：`get_cronpilot_sign()` 保持不变（函数本身不涉及 log\_id 语义）
- **路由路径变更**：`/job_log_detail`、`/job_log_all_list` 等 URL 路径保持不变

## 5. 分批

| 批次 | 内容 | 验收方式 |
| --- | --- | --- |
| **B1: Model + 迁移** | 3 个 Model 文件属性重命名 + `ensure_business_tables.py` 列迁移 | `cronpilot.sh test` 确认编译通过（部分测试会因引用旧名而 fail，预期行为） |
| **B2: 服务 + Repo + 测试** | `crons.py` / `job_health_service.py` / `job_log_repository.py` / 5 个测试文件 | `cronpilot.sh test` 全通过（0 新增失败） |
| **B3: API + Schema** | `api/views.py` / `api/schemas.py` | API 测试通过 + `/docs` Swagger 页面参数名正确 |
| **B4: View + 模板 + UI** | `main/views.py` 模板变量 + 8 个模板文件 + CSS class | restart → 浏览器验证列表 "#" 列头 + 详情页标题用数字 ID + 元数据显示"追踪码" |
| **B5: 配置 + 文档** | `.cursor/rules/` / `AGENTS.md` / `README.md` / `RELEASE_NOTES.md` | `html_docs_to_markdown.py --check` 通过 |

## 6. 验收

### 6.1 自动化验收

```
# 全量测试
bash scripts/cronpilot.sh test

# 旧名残留检查（应无结果）
rg "\.log_id\b" datas/model/ app/ --glob '*.py' | grep -v '__pycache__' | grep -v 'test_job_log_outcome'
rg "cronpilot_log_id" app/ --glob '*.py'
rg "LOG ID" app/templates/

# 数据库列名验证
sqlite3 datas/job_log.sqlite "PRAGMA table_info(job_log);" | grep trace_id
sqlite3 datas/job_log.sqlite "PRAGMA table_info(job_log_items);" | grep trace_id
sqlite3 datas/job_log.sqlite "PRAGMA table_info(job_health);" | grep last_run_trace_id

# 文档同步
python scripts/html_docs_to_markdown.py --check
```

### 6.2 浏览器验收

| 步骤 | 操作 | 期望结果 |
| --- | --- | --- |
| 1 | 访问执行记录列表 `/job_log_all_list` | 第一列列头显示 `#`，非 `LOG ID` |
| 2 | 点击某行进入详情页 | 页面标题为 `执行记录 #18463`（与列表序号一致），非 UUID 前缀 |
| 3 | 查看详情页元数据区域 | 显示 `追踪码` 标签 + 完整 UUID，可复制 |
| 4 | 查看面包屑导航 | 显示 `#18463`，非 UUID |
| 5 | 新建/编辑任务，查看 URL 提示文案 | 提示中显示 `cronpilot_trace_id`，无"回调"字样 |
| 6 | 触发任务执行，检查目标 URL 收到的参数 | 参数名为 `cronpilot_trace_id`（非 `cronpilot_log_id`） |
| 7 | 调用 `POST /api/cron/add_log`，使用新参数名 | `cronpilot_trace_id` 参数被正确识别 |

### 6.3 v1 模板验收

| 步骤 | 操作 | 期望结果 |
| --- | --- | --- |
| 1 | v1 执行记录列表（非 redesign） | 列头显示"追踪码"，显示 UUID 值 |

## 7. 风险

| 风险 | 概率 | 影响 | 缓解措施 |
| --- | --- | --- | --- |
| SQLite `ALTER TABLE RENAME COLUMN` 需要 3.25.0+（2018-09-15） | 低 | 中 | Python 3.8+ 内置 SQLite 均 ≥ 3.25；迁移脚本增加版本检查 |
| MySQL `ALTER TABLE CHANGE COLUMN` 对大表加锁 | 中 | 中 | job\_log 表可能较大；文档提示在低峰期执行迁移脚本 |
| 签名算法中参数名变更导致旧签名失效 | 确定 | 低（无已知外部集成） | RELEASE\_NOTES 标注 `⚠️ API Breaking Change` |
| 漏改某处引用导致运行时 AttributeError | 中 | 高 | 全量测试 + `rg` 旧名残留检查 + import smoke test |

**⚠️ API Breaking Change**：定时触发参数名从 `cronpilot_log_id` 改为 `cronpilot_trace_id`，签名计算随之变化。已部署的目标服务如有验签逻辑，需同步更新参数名。`POST /api/cron/add_log` 接口入参名同步变更。

## 附录 A：受影响文件完整清单

### 代码层（~20 文件）

```
datas/model/job_log.py
datas/model/job_log_items.py
datas/model/job_health.py
app/crons.py
app/api/views.py
app/api/schemas.py
app/main/views.py
app/repositories/job_log_repository.py
app/services/job_health_service.py
app/services/job_log_display.py
app/logging_config.py
scripts/ensure_business_tables.py
tests/test_cronpilot_sign.py
tests/test_job_log_id.py
tests/test_job_health.py
tests/test_api_scope_s6.py
tests/test_rbac_phase.py
```

### 模板层（8 文件）

```
app/templates/redesign/execution_logs.html
app/templates/redesign/run_inspector.html
app/templates/redesign/_exec_logs_rows.html
app/templates/redesign/task_form.html
app/templates/job_log_all_list.html
app/templates/job_log_list.html
app/templates/cron_add.html
app/templates/cron_edit.html
```

### 配置 / 文档层（~5 文件）

```
.cursor/rules/cronpilot-project.mdc
AGENTS.md
README.md
RELEASE_NOTES.md
doc/design/执行记录标识符与术语统一设计.html（本文件）
```

## 实施复盘：views.py 方法调用未同步更新

| 要素 | 内容 |
| --- | --- |
| Bug 定位 | `app/main/views.py` 第 703/716/738 行：`repo.get_by_log_id()` 和 `repo.items_for_log_id()` 在 B2 已将 Repo 方法重命名为 `get_by_trace_id` / `items_for_trace_id` 后仍使用旧名，用户访问详情页触发 `AttributeError` |
| 根因 | B2 批次改了被调用方（Repo），B4 批次改了调用方的其他部分（模板变量、URL 参数），但调用方对 Repo 方法名的引用落在两个批次的 gap 中，未被任一批次的变更清单覆盖 |
| 测试漏洞 | 644 个单元测试不覆盖 `job_log_detail` 路由的端到端渲染（View → Repo 跨模块调用链） |
| 修复 | 3 处 `items_for_log_id` → `items_for_trace_id`，1 处 `get_by_log_id` → `get_by_trace_id` |
| 防护测试 | 全仓库 grep 确认无残留旧方法名：`rg "_log_id\(|_for_log_id|by_log_id" app/ --glob '*.py'` |
| 同类排查 | 已确认：全仓库无其他 `_log_id(` / `by_log_id` / `_for_log_id` 残留 |
| 预防方案 | 跨批次重命名后，须 `rg` 全仓库确认旧方法名不存在于任何调用方。规则追加到 `.cursor/rules/cronpilot-project.mdc`「连续 StrReplace 后必须读回验证」章节 |

[文档索引](index.html) · [Markdown](执行记录标识符与术语统一设计.md) · [索引](index.html)

---

[← 文档索引（HTML）](../index.html) · [← 文档索引（Markdown）](../index.md)
